---
title: "Группа оплаты свежей предброни — пара или трое, четверо значит починка не держит"
state: waiting
review_by: 2026-09-29
stamped_at: 2026-09-11
entry: "~/dev/gmb_v2 (ящик проекта недоступен: основной чекаут отцеплен нарочно, main держит ворктри deploy-main) · ~/dev/gmb_v2 · main · .claude/handoff/fix-prebooking-payment-group-is-the-pair.md"
host: local
cwd: ~/dev/gmb_v2
probe: |
  scripts/prod_sql.sh <<'SQL'
  SELECT ep.payment_group_id, count(*) AS strok
  FROM event_participant ep
  WHERE ep.payment_group_id = (
    SELECT ep2.payment_group_id
    FROM user_transaction ut
    JOIN event_participant ep2 ON ep2.id = ut.event_participant_id
    WHERE ut.kind = 'prebooking' AND ut.status = 'completed'
    ORDER BY ut.id DESC LIMIT 1)
  GROUP BY 1;
  SQL
ripe_match: "(?m)^\\s*\\S+\\s*\\|\\s*[1-9][0-9]*\\s*$"
ripe_when: "у СВЕЖЕЙ предброни, чья группа ещё жива, стало видно число строк в платёжной группе: 2 (пара) или 3 (якорь с гостем) — починка держит; 4 и больше — дефект вернулся"
sample: "pending. Прогнан 07.09 в 11:25 — 0 строк, и это НЕ «всё хорошо»: проводка 11583 на месте, но группу 8233 крон снял ещё в 09:32, payment_group_id у строк пуст, а `= NULL` не сходится ни с чем. Проба исполнима и синтаксически верна, но заговорит только на свежей предброни — до истечения срока доплаты. Положительного исхода не было"
acked_at: 2026-09-22
acked_outcome: fired
acked_key: 1d0760c4
---

**Вторая половина пункта 11**, вынесенная отдельной строкой: у пункта два хвоста,
и они созревают от РАЗНЫХ событий. Первый (inline-чек Т-Банка) — строка
`home/20260911-11`; тело пункта целиком лежит там, здесь только машинная половина.

🔴 **Пустой ответ ничего не доказывает.** Ноль читать как «случая ещё не было», а
не «всё хорошо»: группа протухшей предброни снимается кроном, и `= NULL` не
сходится ни с чем.

**Сработало, когда** запрос вернул строку и `strok` = **2** (пара) — или 3, если
у якоря есть гость. **4 и больше — починка не держит**, дефект вернулся; входить
через `.claude/handoff/fix-prebooking-payment-group-is-the-pair.md`.

⚠️ **Соседняя строка спрашивает то же с другой стороны и её не заменяет.** Строка
из пункта 22 кричит только на ДЕФЕКТЕ (группа шире пары, `HAVING count(*) > 2`)
и никогда не говорит «починка подтверждена». Эта — наоборот. Держать обе.

ОТЛОЖКА.md, пункт 11 (вторая половина), заведён 06.09.2026
