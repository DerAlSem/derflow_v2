# derflow — changelog

**История, не живой документ.** Здесь лежит «почему так решили»; «что истинно
сейчас» живёт в `SKILL.md` и движках `_*.md`. Разделение — применение правила
v3.1 к самому derflow: датированная история не должна притворяться живой спекой
и не должна грузиться в контекст на каждой задаче.

Каждая запись — оплаченный боевой промах. Не удалять при рефакторинге: это
единственное место, где сохранена цена правила.

Текущая версия: **v5.0** (derflow_v2). (До форка `SKILL.md` объявлял v4.14 — он меняется
редко: правка маршрутизации требует полного цикла `writing-skills`, а движки
растут отдельно. Расхождение осознанное, а не отставание.)

---

## v5.1 — docflow, пилот на GM+ (24.09.2026)

Новый скилл `docflow`: docs-first поверх Backlog.md (MrLesk, 1.52.0).
Колонки Backlog / To Do / In Progress / Done; «как устроено» — в
`backlog/docs/capabilities`, а не в закрытых тикетах. Скрипты:
`bl-lock.sh` (атомарный замок в общем git-каталоге, виден всем ворктри),
`bl-done.sh` (задача не закрывается без правки доки capability),
`bl-wake.py` (пробы колонки Backlog: кэш на старте + фоновое обновление),
`bl-migrate.py` (openspec + отложка → Backlog.md). Роутер отправляет в
`docflow` любой репо с `backlog/config.yml`. Пилот — GM+, две недели.

---

## v5.0 — derflow_v2 (24.09.2026)

Форк под Opus 5.5 и расход токенов. Роутер `SKILL.md` ужат с 26,9 до ~12 КБ:
убраны повторы анонс-контракта, управление правкой скилла вынесено в
`_editing.md`. Новое: модель в строке анонса и условие «готово»; исполнители
`python-pro`/`frontend-developer` на Sonnet (были `inherit`); B получает
мини-гейт вместо полного `_gates.md` (полный — при внешнем контракте или
размещении UI); `_capture.md` грузится по поводу, а не в конце каждой полосы;
правило остановок в глобальном `CLAUDE.md`. Снят плагин `marketing-skills`.

⚠️ Правка маршрутизации прошла БЕЗ RED-фазы `writing-skills` — её сделали вне
Claude Code. Долг: прогнать давящие сценарии на B/C/Dx до того, как считать v5
каноном. Замер эффекта — `scripts/context-spend.py` неделя к неделе.

---

- **v4.39: граница полос — это граница СЕССИЙ, и предикат к ней замерен.**
  14.09.2026. Канон говорил «каждая принятая находка перезаходит как A/B/C», но
  не говорил ГДЕ, и все читали это как «новой строкой анонса здесь же». Замер
  137 сессий с двумя анонсами: ПОСЛЕ второго анонса ушло $29 000 из $55 658 —
  больше половины счёта. Разрез по полосе второго анонса дал восьмикратный
  разрыв: A $26 и D $34 за сессию против B $243, C $198, Dx $309, F $679.
  Отсюда правило «кроме A и D — новой сессией», и порога по контексту в нём нет:
  контекст на том же месте предсказывает втрое слабее.

  Оговорка, с которой правило заходило («у B-фикса на три строки перезаход
  дороже фикса»), оказалась неверной в первой половине: B вторым анонсом — это
  682 хода, а три строки — это A. Первый же контроль это и вскрыл, назвав
  однострочник B и расщепившись; A привязана к нижней строке таблицы B/C.

  Потолок веера переведён из токенов в воркеров и деньги: токенная форма
  занижает счёт примерно в сто раз — воркер платит контекст на каждом из своих
  50–100 ходов, а не один раз за задание — и потому не тормозит. RED показал
  это дословно: оба воркера посчитали по канонной формуле и выдали 1.6M и 2.35M,
  числа ни о чём.

  И главное про форму правки: две RED-репы из трёх не открыли ни одного файла.
  Отказ был не рационализацией, а невидимостью — вопрос не вставал. Поэтому
  правило получило машинный носитель: `context-meter.py` стреляет на СОБЫТИИ
  второго анонса, а не по уровню 300k (медиана на втором анонсе — 182k, порог
  успевал сработать в 27 сессиях из 137).

  Добор 15.09: цена воркера `$7` была взята из ОДНОГО веера и вписана в канон
  точкой. По всему корпусу — 918 воркеров, среднее $5, дециль 9 — $10; якорь
  сидел около 75-го процентиля. Свой же урок «утверждение проверяется данными»
  я применил к форме потолка и не применил к его константе.

  Порог 300k по тому же добору оставлен как есть: медиана ходов после перехода
  упала 794 → 80, но отрицательный контроль запретил приписывать это порогу —
  сдвинулось всё сразу. Жалоба замера смешала две работы: различать полосы —
  дело детектора второго анонса, тормозить по длине — дело уровня.

  Рассказ и границы — `archive/lane-cost-rules-promoted.md`, числа и три убитые
  гипотезы — `archive/lane-cost-measured.md`.

- **v4.38: проба не той ФОРМЫ дала ложное оправдание.** 12.09.2026. Проверяя,
  не моя ли смена транспорта `hand.sh` обнулила PATH порождённой сессии, я задал
  пробе `command` как `zsh -c '…'`. Ghostty увёл такую строку через
  login-оболочку (`$0` = `-zsh`), homebrew в PATH оказался, и я записал в канон
  «транспорт поломку не создаёт». Боевая доставка отдаёт `command` иначе —
  ПУТЁМ К СКРИПТУ, — и тогда `$0` равен самому скрипту, оболочки-обёртки нет, а
  PATH ровно тот голый, что видела сестра. Виноват был транспорт, то есть я.

  Признак: проба и боевой вызов отличаются ФОРМОЙ аргумента, а не только
  данными. Лечение — воспроизводить форму вызова буквально, вплоть до того, чем
  задан аргумент; ответ «всё хорошо», полученный дешевле боевой формы, не
  ответ. Названный отказ: «проба прошла, значит не мы» — самое дорогое место,
  потому что оправдание перечитывается как уже согласованное и второй раз не
  проверяется (тот же класс, что v4.8 про «битых указателей ноль»).
  Тупик, который стоит исключить сразу: искать причину в чужом коде. Улика
  «отказ старше моей правки» у меня тоже была — таб от давнего неудачного
  лончера, — и она объясняется и без моей невиновности.

- **v4.37: уборка после опыта снесла три таба владельца.** 12.09.2026.
  Разбираясь с доставкой `hand.sh`, я ставил пробные табы в живых окнах владельца
  и убирал их AppleScript'ом. Два независимых промаха сложились: закрытие шло
  ВНУТРИ обхода коллекции (`repeat with t in tabs of w` + `close tab t`), где
  индексы едут под обходом, а `2>/dev/null` на уборке сделал снос невидимым.
  Погибли три таба: две оболочки и `docker`-оболочка, чей контейнер после этого
  не нашёлся ни в `ps`, ни в `ps -a`. Различить «убил» и «был мёртв до меня»
  уже нечем — и это худшая часть цены.

  Счётчик табов сразу после закрытия ВРЁТ: дважды дал 17 и 12 при верных 16 и
  15, потому что AppleScript отвечает раньше, чем окно перестроилось. На нём же
  снос сначала выглядел вдвое большим. Сверка — только перечислением.

  Правило в `_gates.md` рядом с v4.20: там движущаяся мишень портила замер,
  здесь опыт портит мишень. Отдельно названо, что «teardown обязан молчать»
  (`waiting-bite.sh`) сюда НЕ переносится: там глушится названный осадок в своём
  `mktemp`, здесь правилось чужое. Лучшая форма пробы — самоубирающаяся:
  команда пишет результат в файл и выходит при `wait after command = false`,
  тогда уборки не требуется вовсе.

- **v4.36: мутация проехала зелёной, потому что её эффект гасил второй фильтр.**
  12.09.2026. Стенд `toolcarry-bite.sh`, мутация `lt = toks(ln) - ct` →
  `toks(ln)`. Ветвь достижима, строка исполняется и честно меняет `lt` — а
  наблюдаемое число не двинулось: токены самой команды лежат в записи с
  `tool_use`, то есть ВСЕГДА раньше своего вывода, и строгий счёт отбрасывал их
  вторым фильтром, новизной. Вычитание `ct` видно только в МЯГКОМ счёте.

  Снаружи это неотличимо от «сторож смотрит не туда», и принимаются переписывать
  предикат — он был исправен. Правится ВЕЛИЧИНА, на которую он смотрит: укус
  переписан с `strict` на `soft`, и мутация покраснела. Третий вариант к
  v4.21 (недостижимая ветвь) и v4.33 (частичная мутация): здесь ветвь достижима
  и мутация полна, гасит результат условие НИЖЕ по ходу.

  Гейт: 14 укусов, 13 мутаций, каждая роняет ровно свой укус; исходное зелёное
  до и после каждой.

- **v4.35: расщепление открывало таб в чужом окне — доставка надеялась на
  фокус.** 12.09.2026. Владелец: «хендофф открылся не соседним табом, а вообще
  в другом окне». Родитель при этом был опознан ВЕРНО — в транскрипте родителя
  стоит `родительский таб выбран по tty (/dev/ttys009)`.

  Словарь Ghostty различает две вещи, а скрипт знал одну: `select tab` выбирает
  таб В ЕГО ОКНЕ и окно вперёд не выводит, а `open -a` отдаёт документ
  ПЕРЕДНЕМУ окну. Пока родитель и фокус человека совпадали, подмена не
  замечалась; скрипт при этом печатал «новый встанет рядом», обещая гарантию,
  которой не имел. Ghostty 1.3.1 умеет `new tab in <окно> with configuration` —
  назначение задаётся явно, фокус в расчёте не участвует. Замер: два пробных
  таба легли ровно в названные окна, включая непереднее.

  Вскрылось тем же опытом и второе: `open -a Ghostty.app <файл>` умеет вернуть
  0 и не сделать ничего наблюдаемого — а через много минут приехать НОВЫМ ОКНОМ.
  Первая редакция записи назвала отказ тихим; неверно, он отложенный и не той
  формы, и скрипт его увидеть не может вовсе.

  🔴 **Эта правка внесла регрессию, и первая проба меня ложно оправдала.** Смена
  транспорта сменила и ОКРУЖЕНИЕ запуска: `open -a` отдавал Ghostty документ, и
  тот открывал его login-оболочкой, а `set command of cfg to <путь>` исполняется
  НАПРЯМУЮ — `$0` равен самому скрипту, PATH голый
  (`/usr/bin:/bin:/usr/sbin:/sbin:…Ghostty.app/Contents/MacOS`), `claude` не
  находится, расщепление молча не стартует. Поймала это сестринская сессия в
  бою через час (`ab07a56`: PATH родителя в лончер плюс абсолютный путь к
  клиенту) — исправление верное и уже в стволе.

  Ложное оправдание разобрано отдельно в v4.38: первая проба задавала `command`
  как `zsh -c '…'`, Ghostty увёл её через login-оболочку, homebrew в PATH был —
  и я записал «транспорт не виноват». Улика «таб `…/T/derflow-hand` стоял ещё до
  правки» тоже была перечитана как доказательство, хотя объясняется и иначе.
  Проверял я РАЗМЕЩЕНИЕ таба и ровно его и проверил; «стартует ли сессия» было
  объявлено пробелом — и пробел оказался несущим.

- **v4.34: счётчик контекста был слеп к автономному прогону.** 12.09.2026.
  Владелец спросил, почему свип спеки в `V·хвосты слияния` сжёг столько токенов.
  Разбор транскрипта: свип ни при чём — все результаты инструментов за сессию
  345 тыс. знаков. Счёт сделала ДЛИНА: одна человеческая реплика, 404 хода,
  контекст 55k → 488k, cache-read 113,5M, последняя треть ходов — 48% счёта.

  `context-meter.py` висел только на `UserPromptSubmit`, то есть мерил по
  событию человека. В автономном прогоне человек молчит — метр отработал один
  раз, на 55k. Добавлен вход на `PostToolUse` с посессионной защёлкой: один
  раз за сессию, с названным рычагом `hand.sh`, а не ссылкой на главу.

  Гейт: стенд `ctxmeter-bite.sh` заведён с нуля, 7/0; заглушка 0/7, версия до
  правки 1/6. Первая редакция стенда давала заглушке 5/8 — отрицания стояли
  порознь; спаяны с положительными в тех же утверждениях.

- **v4.33: аварийное имя таба выглядело штатным, а омоглиф проезжал гейт.**
  12.09.2026. Владелец спросил, что означает буква `T` в тайтле живой сессии
  `T·fix/team-roster-releases-the-cancelled`. По карте `T` — это `cpvs-tbank`,
  но буква пришла НЕ из карты.

  **Фолбэк нарушал своё же требование.** Канон объявляет его «нарочно тупым, он
  не должен выглядеть достаточным», а строил как `<первая буква каталога>·<ветка>`
  — то есть в форме ЗАКОННОГО имени. Ворктри `team-roster-releases` дал букву
  `T`, и таб стал неотличим от сессии `cpvs-tbank`. Предупреждение на stderr не
  спасло: оно там и было. Хендофф с канонным `имя: G·состав отпускает снятых`
  написан на 19 минут ПОЗЖЕ рождения таба и назвать его не мог. Теперь фолбэк —
  `?·<ветка>`: проектом не притворяется ни при каком каталоге. Отсутствие строки
  осталось предупреждением (решение владельца 05.09.2026 не пересматривалось).

  **Омоглиф.** `Т·` и `T·` в табе — один и тот же знак, а буква стоит первой
  ровно затем, чтобы таб опознавался по первому знаку. Отвергается кодом 4.
  Карта проектов в скрипт по-прежнему НЕ копируется: набор двойников — свойство
  алфавитов, а не проектов. `Л` проезжает, и это укус: запрет на всю кириллицу
  снёс бы `muzloto`.

  Граница названа вслух: `Г·планировщик-от-бота` скрипт не ловит и ловить не
  может — канон сам разрешает букву не из карты, значит кириллица без двойника
  законна по форме. Это остаётся на глаз.

  Гейт: до правки 17/2, после 19/0, заглушка 1/18.

- **v4.32: починили громкую половину дефекта — тихая осталась, и сторож был
  мёртв.** 12.09.2026. Строка реестра `rk_bot/20260911-01` — сторож ухода часов
  ККТ, ждущий первой боевой эскалации. В шапке два независимых дефекта.

  **Громкий:** `grep -c` без совпадений выходит кодом 1, `probe_one` читает
  любой ненулевой код как «недостижима». Честно молчащий сторож печатался
  отказом транспорта — и этот симптом привёл к разбору, то есть реестр
  отработал.

  **Тихий:** `ripe_match: ">0"` — литеральная регулярка, а не сравнение. Ни с
  каким счётчиком она не совпадает, поэтому в день настоящей эскалации строка
  сказала бы «молчит». Созреть сторож не мог никогда.

  🔴 Тихую половину не показал бы никто по построению: весь смысл реестра —
  различать «событие не наступило» и «спросить не смогли», а предикат, который
  не может совпасть, выглядит ровно как первое. Тишина у сторожа — штатное
  чтение, и замечать в ней нечего.

  Спека обе половины называла верно («`probe` обязан выходить нулём»,
  «`ripe_match` — регулярка») и не поймала ни одной: проза не срабатывает,
  срабатывает форма. Поэтому отказ заведён в `faults()` и укушен — узкий,
  только «знак и число целиком», чтобы не снести законные `^[1-9]`; под него не
  попадает ни одна из 34 нынешних проб, а якорный литерал `^>0$` проходит.

  Тупик, исключённый сразу: починить то, что загорелось, и уйти. Замерено —
  после правки одной громкой половины `doctor` кодом 0, строка `silent`, группа
  «недостижима» пуста. Всё зелено, сторож мёртв.

- **v4.31: замер выбирал число, не умея ответить на вопрос — и стенд охранял
  домысел реализации.** 11.09.2026. `CACHE_TTL_S` реестра ожидания стоял с
  пометкой «временное число, выбирается замером». Замер накопился (441 прогон),
  и оба его слоя оказались негодными.

  **Слой 1: датчик врал сдвигом, растущим с размером данных.** Первый прогон
  строки сравнивался с отсутствующим прежним исходом и засчитывался СМЕНОЙ —
  по одной фантомной смене с каждой заведённой строки. 39 «смен» при 33
  строках, настоящих 6. Чем больше реестр, тем убедительнее ложное число.

  🔴 **Стенд этот домысел охранял** — укус утверждал «первый прогон — смена
  исхода», хотя спека говорила лишь «прогон со сменой исхода», что предполагает
  наличие прежнего. Отсюда правило главы: при расхождении укуса и спеки первым
  подозревай укус — он пишется позже и часто фотографирует поведение.

  **Слой 2: даже честный замер отвечал не на тот вопрос.** Доля смен — про
  частоту событий, TTL — про допустимую задержку обнаружения. Из «события
  редки» одинаково следует и «опрашивать реже», и «тем важнее не проспать».
  Никакой объём данных одну величину в другую не превращает. Добивает третье:
  четыре из шести настоящих смен дала строка, чья проба читала сам замер.

  Решение владельца: число остаётся 3600, но перестаёт быть временным —
  основание записано вслух (дельта на старте сессии не старше часа, цена
  обратной стороны замерена холодным кругом 13,4 с). Прежняя формула спеки
  «частое „то же самое“ значит TTL тесен» названа ошибкой и заменена; ту же
  подпись печатал `doctor` — поправлена и она.

  Тупик, исключённый сразу: копить ещё неделю данных бесполезно — это лечит
  слой 1 и не трогает слой 2.

- **v4.30: «рабочий каталог сбрасывается» оказалось неверным — он персистит, и
  правило держалось на ложном доводе.** 11.09.2026. `_parallel.md` с давних пор
  требовала абсолютных путей в пишущих командах, объясняя это тем, что рабочий
  каталог между вызовами инструмента **сбрасывается**. Замер показал обратное:
  после `cd` внутрь рабочего каталога следующий вызов стартует ТАМ ЖЕ и молча;
  сброс приходит только при выходе ЗА рабочий каталог, и тогда харнесс печатает
  `Shell cwd was reset to …`.

  **Обоснование было зеркальным, вывод — верным.** Команда уезжает не туда не
  потому, что каталог сбросился, а потому, что он ОСТАЛСЯ уведённым. Правило
  («абсолютный путь либо `cd` в том же вызове») не изменилось ни на знак — и
  это не делает правку косметической: пока довод ложен, его применяют по вере,
  а не по механизму, и первый же случай, где вера не срабатывает, проезжает.

  🔴 **Названный отказ хуже, чем просто неверный факт: предупреждение приходит
  ровно в безобидном случае.** Выход за рабочий каталог печатается вслух —
  а он как раз безвреден. Кусающийся случай (каталог остался внутри дерева)
  проходит молча. Значит «сбросится само» не сработает никогда именно там, где
  на это надеешься.

  Куплено в тот же день: после `cd skills/derflow` два вызова `waiting.py done`
  ушли кодом 2 и **не погасили ни одной строки реестра**. Спасло лишь то, что
  отказ был громким («can't open file»); на месте `done` мог стоять `git add -A`,
  и он бы тихо отработал в чужом каталоге — ровно та поломка, ради которой
  правило и писалось, только с обратным знаком.

  Тупик, который стоит исключить сразу: **`cd` не «не сработал»** — он отработал
  оба раза. Переписывать команду бесполезно; слепо было ожидание чистого старта.

- **v4.29: уроки выведенной отложки расселены по домам — знание жило только в
  git-истории.** 11.09.2026. Реестр ожидания (`waiting.py`) заменил
  `ОТЛОЖКА.md`, и 24 ждущих пункта переехали строками. Но ~190 строк файла
  работой не были — это были уроки, а знание реестр не хранит. Их оставили одной
  строкой-долгом `home/20260911-09` с названной вслух ценой промедления: **git
  их хранит, но не подаёт**, и спросить историю выведенного файла никто не
  догадается. Расселение по трём движкам и в память:

  **`_capture.md` — новая глава «Опрос сестринских сессий».** Шесть отказов
  приёма, все замеренные. Главный: **«не ответила» — два разных состояния, и на
  глаз они одинаковы.** `gmb-v2-ef` отработала опрос за 18 секунд и написала
  полный ответ себе в терминал обычным текстом — до спрашивающего не дошло
  ничего. Различает их только греп транскрипта по `msg_id`. Рядом: молчание
  есть отсутствие факта (строка была написана по устаревшему хендоффу, пока
  сессия вела живой разговор); простаивающая сессия разбирает очередь лишь на
  своём следующем ходу; `started` в `ListAgents` — метка старта, а не простоя
  («waiting, 4d» у сессии, с которой владелец говорил минутами раньше); имя
  сессии протухает; опрос достаёт **только интерактивные** сессии, а двадцать
  пять `Remote Control · offline` недостижимы. Туда же — правило заводить строку
  ожидания только спросив, не делается ли это сейчас: строка «Стекло» прожила
  часы, потому что событие было у владельца под рукой.

  **`_parallel.md` — четвёртая поверхность: ворктри не граница СЕССИИ.** В одном
  `.claude/worktrees/face-control-toggle` на одной ветке жили две линии разом;
  коммит `280c9b5b8` одной лёг поверх двух другой, и `land.sh` приземлил его как
  свой. Не затёрли ничего только потому, что правили разные файлы. Прежние три
  поверхности были общими на репозиторий — эта общая на дерево, и заголовок
  главы перестал обещать ровно три.

  **`_gates.md` — таймаут CDP-скриншота, и виновата НЕ нагрузка.**
  `Page.captureScreenshot` встаёт на тридцати секундах при живом JS той же
  вкладки, замерено четырежды за две сессии при load 3.67. Прошлая сессия
  списала это на load 25–35 и **записала догадку в хендофф** — ложное
  объяснение доехало до следующей сессии и стоило ей тех же четырёх попыток.
  Обход: свой http-сервер с обязательным `charset=utf-8` (без него кириллица
  мохибейком и замеренные ширины врут) плюс `getBoundingClientRect`.

  **Память — отклонённая рекомендация тоже знание.** На `derserver` глобально
  разрешён парольный вход по ssh при пробросе наружу; владелец от рекомендации
  отказался 06.09.2026. Без записи находка открывается заново каждым, кто зайдёт
  в администрирование сервера, и каждый раз оформляется как свежая уязвимость.
  След отказа в коде не остаётся — потому он и теряется быстрее принятого.

  **Что здесь общего и почему это правило, а не четыре довеска:** выведенный из
  обращения файл уносит с собой не только работу, но и знание, а знание
  переезжает **по вопросу, на который отвечает**, а не следом за работой. Урок о
  процессе — в движок, факт о чужой машине — в память со сроком, отклонённая
  рекомендация — туда же и с той же силой, что принятая.

- **v4.28: ручной путь расщепления был дырой во всех трёх местах сразу.**
  07.09.2026. Владелец показал таб `✳ Продолжение handoff` посреди честно
  названных `G·отложка`, `G·галка`, `G·текст` и назвал три жалобы: подтверждение
  на открытие таба спрашивается каждый раз, имя ручной сессии бессмысленно,
  файлы хендоффа зовутся одинаково и по-русски. Корень у всех трёх один и тот
  же — **автопуть чинили, ручной не трогали вовсе**.

  **Подтверждение.** Спрашивал не macOS, а сам Claude Code: под `hand.sh` не
  было allow-правила, `"allow"` в `settings.json` стоял пустым. Отработавший
  молча `osascript -e 'tell application "Ghostty" …'` доказал, что
  Automation-доступ выдан давно, — TCC второй раз и не спрашивает. Добавлены две
  строки allow (абсолютная и `~`-форма), и канон обязал звать полным путём без
  конвейера: правило сверяется с началом команды, а `… | tail -20` добавляет
  вторую команду и снова просит подтверждения. Прежние вызовы почти все шли
  через `| tail`, то есть правило само по себе не помогло бы.

  **Имя.** `hand.sh` кормит преемника КОНСТАНТНЫМ промптом, а имя вёз только
  флаг `--name`. Но Claude Code генерирует имя сессии **из первого промпта**,
  когда флага нет (`generateSessionName`, 2.1.236) — значит на ручном пути
  автоимя было пересказом константы и выходило одинаковым у всех расщеплений.
  «Продолжение handoff» — не сбой, а буквально это. Починка в одну строку:
  имя переехало ВНУТРЬ константы, и ручной путь стал называться по задаче
  без всякого нового механизма.

  **Ручной путь вообще не должен возникать.** `hand.sh` принимает каталог первым
  аргументом, то есть открывает накормленный таб в ЛЮБОМ дереве. Значит «создай
  ворктри и возьми там хендофф» было не удобством, а обходом скрипта — и ровно
  оно породило безымянный таб. Канон запретил три формулировки одного корня:
  `/clear`-с-вставкой, «открой ворктри», «зайди и запусти `claude`».

  **Имя файла.** Ключ живёт в шапке (v4.24), имя свободно — но свобода не значит
  «любое»: кириллица уже стоила молча инертного гейта свежести, а `HANDOFF.md`
  на десяток файлов не различает ни одного. Канон — латинский слаг задачи;
  `HANDOFF.md` законен только внутри каталога заявки, где различает каталог.

  📏 **Стенд укуса вырос с 11 до 14 и покраснел на всех трёх.** На скрипте до
  правки — 2 красных, на промежуточном варианте — 1, и это тот самый укус,
  который поймал ложное срабатывание: первая редакция гейта латиницы была
  `case ... *[!\ -~]*`, и экранированный пробел ломал диапазон так, что шаблон
  совпадал с ЛЮБЫМ именем — `tab-name.md` включительно. Проверка «есть ли
  подстрока» это считала успехом, поэтому в `chk` добавлена **обратная иголка**
  (`!` в начале = подстроки быть НЕ должно). Гейт, срабатывающий всегда,
  неотличим от работающего, пока стенд умеет проверять только присутствие.

- **v4.27: «ворктри не видят память» оказалось неправдой — чинить надо было чекер.**
  06.09.2026. Пункт стоял в каноне известным дефектом и в хендоффе — единственным
  живым: выбор механизма сшивки (общая память на репозиторий против «читать
  основную, писать к себе») считался несделанным. Перед выбором дефект проверили,
  и он не подтвердился: на Claude Code 2.1.236 встроенная механика **сама
  разрешает ворктри в основное дерево** и называет сессии путь основной памяти.
  Выбор сделан выше по стеку и в пользу общей памяти; строить было нечего.
  Готовый `scripts/wt-memory.sh` (симлинк `memory` на основное дерево) выброшен
  до коммита — механизм под несуществующий дефект.

  **Замер — три прогона `claude -p`, и третий из них решающий.** Плоский не-git
  каталог: свой слаг, индекс не загружен. Ворктри без симлинка: слаг ОСНОВНОГО
  дерева, индекс загружен. Он же с симлинком: то же самое. Без первой строки
  второй результат читался бы как «механика всегда показывает основную память» —
  вывод вышел бы о замере, а не о ворктри.

  **Наша половина всё же была сломана — `check.py`.** Он считал слаг сам, по
  `--show-toplevel`, а тот в ворктри отдаёт ворктри: отчёт печатал `ПАМЯТЬ НЕ
  ПРОВЕРЕНА` там, где память есть. Теперь предикат тот же, что у механики, —
  `--git-common-dir`; в живом ворктри `~/.claude` отчёт стал видеть память
  (2 файла вместо «не проверена»). Самотест получил укус на ворктри с двумя
  отрицательными контролями: не-git каталог слаг не подменяет, а `--memory-dir`
  перебивает механизм.

- **v4.13 (SKILL.md): `dertext` отменён, нативность ушла в `ru-text`.**
  06.09.2026, решение владельца: «рутекст работает лучше, убрать полностью, как
  будто и не было». Два скилла на один русский текст были разведены по вопросу
  (`ru-text` — норма, `dertext` — слух), и разведение стоило лишнего прохода на
  каждом человеческом тексте. Снято: цель маршрутизации одна.

  Отвязано разом во всех местах, где сидела развилка, — `SKILL.md` (строка
  дисамбигуации и правило про доки), `CLAUDE.md` (глава «Качество русского
  текста»), `_capture.md` (пример кросс-проектного скилла), `_lane-c.md`
  (маркетинговый слой), `README.md`, `agents/VENDOR.md` — и каталог
  `skills/dertext/` удалён: пока файл лежит на месте, скилл остаётся в списке
  доступных и срабатывает по описанию, то есть «отменён» был бы только на бумаге.
  Корпус на 63 КБ остаётся в истории git.

  ⚠️ **Это правка маршрутизации без RED-фазы на сабагентах — сознательно.**
  Цикл `writing-skills` доказывает, что новое правило кусается под давлением; тут
  правило не добавлено, а **снято**, и проверка у снятия другая: ноль висящих
  ссылок по `grep` во всём репозитории. Она пройдена.

- **v4.26: соседство таба искали по имени преемника — а родитель назван своей задачей.**
  06.09.2026. Механизм v4.21 (выбрать родителя перед открытием) прошёл фикстуру
  05.09 и промахнулся в бою назавтра. Разбор транскрипта родителя: `⚠️ таба
  «C·память в ворктри» нет` — скрипт отработал ровно как написан, неверна была
  **посылка**. Имя берётся из `имя:` хендоффа и описывает СЛЕДУЮЩУЮ задачу;
  родительский таб назван своей, и совпадают они только в цепочке «та же задача
  дальше». Расщепление затем и делают, что задача другая. Фикстура не ловила
  этого by design: там имя задавалось обоим.

  **Починка сняла ложный исключённый тупик.** v4.21 записала «метку через OSC не
  послать — у Bash под агентом нет TTY». Нет только УПРАВЛЯЮЩЕГО терминала
  (`/dev/tty`); само устройство открывается **по пути**, а путь даёт `ps` по
  `$CLAUDE_PID`. Запись заголовка в `/dev/ttysNNN` метит ровно тот таб, из
  которого запущен `hand.sh`, — то есть родителя, а не однофамильца.

  **Метка живёт секунды: её стирает сам Claude** (значок `✳`/`◐`/`◑`
  перерисовывается постоянно). Один прогон из пяти терял её между записью и
  чтением; после переписывания метки перед каждой попыткой — 6 из 6.

  🔴 Цена, оплаченная в этом же прогоне: пустой снимок заголовков (Ghostty не
  ответил по AppleScript) оставил таб с `derflow-parent-…` на экране — пометили
  тем, что нечем снять. Отсюда отказ: **без снимка метка не ставится вовсе.**

  Сквозной замер: выбор нарочно уведён на таб 1, родитель — таб 6, новый встал
  **7-м**. Отрицательные контроли — tty нет и имя одно, ноль совпадений, три
  совпадения — фокус не двинули. `scripts/hand.sh`, `_capture.md` v4.21→v4.26.

- **v4.25: правило формата проиграло формату; срок памяти получил сторожа.**
  06.09.2026. Разбор 01.09 сжал память gmb-v2 со 192 записей до 65 и индекс с
  50.7 КБ до 11.6. За пять дней он отрос до **100 записей и 21.5 КБ** — 84 %
  потолка, при темпе ~2 КБ в сутки. Это и был обещанный исход правила «лечить
  переполненную память уборкой»: уборка купила пять дней, а не недели.

  **Но отросло не то, что чинили.** Записей +54 %, а индекс +82 %: росли САМИ
  СТРОКИ. 57 строк из 100 длиннее 120 знаков, самая длинная — 471. Детали
  дописывались в указатель вместо тела файла, то есть в единственное место,
  которое грузится всегда.

  **Механизм, который должен был это держать, не применялся ни разу.**
  `живёт до:` — в 1 файле из 100 при 60 файлах, тронутых за эти пять суток;
  `supersedes:` — в нуле. Причина не дисциплинарная и вскрылась сразу, как только
  вопрос был задан про доставку, а не про старательность: правило живёт в
  `_capture.md`, который читается **в конце полосы и только если загружен**, а
  форму франтматтера задаёт встроенная инструкция памяти — она в системном
  промпте **каждой** сессии и поля срока не содержит. Побеждает то, что перед
  глазами всегда. Ровно тот же класс, что v4.20 поймала на дрейфе канона, и
  лечение того же класса: контракт формата переехал в глобальный `CLAUDE.md`.
  Исключённый тупик — **дописать правило в движок ещё раз**: его же и не прочтут.

  **Поле без читателя лечением не является** — это «забытый долг с видом
  учтённого», ошибка из списка этой же главы. Поэтому вторая половина:
  `check.py` получил проход `ГИГИЕНА ПАМЯТИ` (вес индекса, строки длиннее 120
  знаков, записи без срока, истёкшие по дате, сироты, битые указатели). Сводкой,
  а не построчно — 99 находок «нет срока» это отчёт, который не читают.
  Целостность после массовой правки иначе и не проверить: 01.09 её сверили
  глазами и отчитались «битых указателей ноль», а их было шесть.

  **Фикстура зеленела там, где живой прогон краснел:** она отдавала набор уже без
  `MEMORY.md`, а `run()` отдаёт вместе с ним — и индекс печатался сиротой самому
  себе. Стенд обязан отдавать набор ТАК ЖЕ, как его отдаёт вызывающий.

  **Побочно оплачено:** `check.py` на старом срезе дерева врёт авторитетно —
  основной репозиторий стоял в detached HEAD на теге пятидневной давности, отчёт
  дал 78 находок, и все проверенные символы нашлись в `main`. И длина строки
  индекса меряется ЗНАКАМИ: `awk length` считает байты, кириллица идёт по два, и
  порог завышается вдвое (720 против 471).

- **v4.24: хендофф ключуется шапкой, а не именем файла; тихий промах подбора
  стал отказом.** 06.09.2026. Дважды за одно утро `hand.sh` скормил преемнику
  **корневой `HANDOFF.md` чужой линии** — оба раза человек увидел это в новом
  табе и переоткрывал сплит руками.

  **Два разных промаха подбора, один общий исход.** У `inbox-push-and-move-trace`
  шаг 2 не нашёл файл: тот был назван по-человечески, а искали по слагу ветки —
  и сессия сделала `git mv` в машинное имя, чтобы попасть в шаблон. У
  `face-control-toggle` шаг 1 не нашёл заявочный хендофф: строка
  `ветка: feature/… (ворктри …)` сверялась целиком, а не первым полем. Оба
  свалились в фолбэк на корень.

  **Несущий дефект — не промахи, а их тишина.** Фолбэк печатал предупреждение в
  `stderr`, и оно проехало мимо глаз. Шаги 1–2 доказывают принадлежность по
  построению; корень не доказывает ничего. Теперь автоподобранный файл без
  строки `ветка:` — отказ кодом 5; выбранный человеком вручную остаётся
  предупреждением. Это **третий подряд** случай, когда stderr не сработал, —
  обобщение поднято в канон: есть тихая ветка исполнения → гейт отказывает кодом.

  **Задвоение породил ключ, а не небрежность.** Ключевание именем файла делало
  человеческое имя невозможным. Ключ переехал в шапку: скрипт сканирует
  `.claude/handoff/*.md` и `handoff/*.md` и берёт тот, чья `ветка:` совпала.
  Больше одного совпадения → отказ кодом 6 со списком.

  🔴 **Стенд укуса нашёл дефект, которого никто не искал: гейт свежести был
  молча инертен ВТОРОЙ раз.** После починки `-uall` (v4.18) осталась слепота той
  же формы — `git status --porcelain` печатает не-ASCII имя как `"\320\277…"`,
  и правка в файле с кириллическим именем гейту не видна; `ПРОДОЛЖИТЬ.md` в
  списке фолбэка сам такой. Изолированная проба: старый скрипт `rc=0`, новый
  `rc=2`. Оба раза дефект был невидим снаружи — скрипт печатал успех. Отсюда
  стенд переехал в репозиторий: `scripts/hand-bite.sh`, 11 укусов, на скрипте до
  правки 9 красных.

- **v4.23: детектор дрейфа держится в бою — 10 из 10 на решениях; закрытие
  сессии закрыто отказом от постройки.** 06.09.2026. Ретро по 218 транскриптам,
  16 срабатываний в 12 сессиях.

  **Цифра и её ловушка.** Голая доля — 7 из 16 перечитали названный файл первым
  действием, 3 позже, 6 не перечитали вовсе. Читать её как «правило держится
  наполовину» нельзя: все шесть промахов легли на промпты, которые не были
  решением («тут закрыто же всё?», «давай команды для винды»), а строка хука
  кончается словами «до следующего решения». На срабатываниях, легших на
  настоящее решение, — **10 из 10**. Опровергающего случая, названного в главе
  v4.20, не нашлось ни одного.

  **Дефект нашёлся не там, где искали.** Проба на стендах давала RED 2/3 и
  предсказывала промах агента под давлением. Бой показал другое: промахивается
  не агент, а предикат — он не знает, работа ли этот промпт, и 38% строк тратит
  на вопросы. Предикат «сессия трогала канон И канон уехал» заработан, предиката
  «сейчас принимается решение» нет; цена теперь измерена, а не предполагается.

  **Метод замера подвёл дважды, оба раза в сторону завышения работы.** Хендофф
  назначил считать `grep -c` по строке хука и получил 160 вхождений в 20 файлах;
  настоящих срабатываний 16 в 12 сессиях. Греп считал эхо ассистента, вывод
  стенда укуса и **след самих ретро-сессий**, плюс одну запись хука дважды
  (`content` и `stdout`). Вторая ошибка — первый анализатор резал `Bash`-команду
  до 110 знаков перед сопоставлением и записал в промах сессию, перечитавшую
  канон диффом против своей кэшированной копии, то есть дешевле всех остальных.
  Обе поправки в каноне отдельными строками: измерение, оставляющее след в
  измеряемом, — общая ошибка, а не разовая.

  **Ожидание было ошибкой формы, и оно названо.** Пункт стоял «смотреть первое
  живое срабатывание», хотя строка печатается в транскрипт, а транскрипты лежат
  на диске: замер был ретроспективен с первой минуты. Сутки ожидания не дали
  ничего сверх ретро.

  **Пункт 6 закрыт без кода — наблюдателя нет.** Искали машинный признак
  закругления сессии. Замер: человек произнёс его вслух **трижды за месяц с
  лишним**, тремя формулировками без единого общего слова, и ноль раз после
  `ac55ae7`. Но решающая причина не редкость, а форма события: закругление обычно
  не произносится вовсе — сессия просто перестаёт получать промпты.
  `UserPromptSubmit` наблюдает приход промпта и **отсутствие промпта не
  наблюдает по построению**; `SessionEnd` в 2.1.236 существует, но стреляет,
  когда человек уже ушёл, и прощальное сообщение адресовать некому. Ни одно
  событие хука не наступает в момент, когда сообщение ещё имеет смысл. Правило
  v4.16 остаётся правилом, которое сессия применяет сама. Потеря затем измерена
  отдельно (167 завершённых сессий): канон расщепления уже съел её почти
  целиком — доля сессий, не уложивших состояние в файл, упала с 57% до 17%, p90
  незаписанной работы с 15 промптов до 1. Разбор восьми оставшихся показал, что
  «не записала хендофф» потерей чаще всего не является: пять кончились без
  долга, две уложили состояние в артефакты заявки и память по правилу границы
  хранилищ. Настоящая потеря одна, и она не про форму прощания — сессия отдала
  долг СО СРОКОМ голосом и не поставила ни триггера, ни отказа. То есть
  предложенная договорённость «говорить закругляемся» её бы не спасла. Дежурство на
  симметричном моменте уже стоит: `↩ возврат через Nч в контекст Nk` ловит
  молча закрытую сессию не на её конце, а на возвращении.

- **v4.22: потолок критик-слоя перемерен — медиана расхода упала вдвое, и дату
  замера побила интенсивность работы.** 06.09.2026. Замер был назначен на 08.09,
  прогнан 06.09 по решению владельца.

  **Цифра.** Медиана контекста на ход главного цикла — **224k** против базовой
  линии **486k**, p90 — 411k против 862k. Доли расхода: cache-read 70%,
  cache-write 18%, output 12%. Разница в 2,2 раза, и правило чтения, записанное
  заранее, велело засчитывать только крупную — эта крупная.

  **Почему не стали ждать назначенной даты.** Четыре дня назначались не ради
  календаря: прошлый замер взял 24–37 завершённых сессий с трёх дней и сам
  назвал окно коротким, а четыре дня были минимумом, который его бьёт. Но
  послепотолочные ДВА дня дали **76** завершённых сессий — вдвое больше того,
  что требовалось побить. Окно замера меряется выборкой, а не сутками; это и
  поднято в канон отдельной строкой, потому что ошибка общая, а не разовая.

  **Что замер по-прежнему не контролирует, названо честно.** Два перекоса живы и
  смотрят в разные стороны: старая база занижена (до v4.11 расщеплённые сессии
  не попадали в транскрипты) — против «стало лучше»; новое окно занижено (живые
  сессии не добрали расход) — за. Ожидание лечило бы только второй, и то
  частично: живые сессии сидят на заднем крае ЛЮБОГО окна, длинное лишь
  разбавляет их долю. Направление вердикта два дня изменить не могли.

  **Побочная находка, которую легко прочесть наоборот.** Доля сабагентов в
  cache-read выросла вдвое, 5% → 11%. Вывод «оптимизировать не сабагентов» она
  не отменяет, но причина роста — похудевший знаменатель, а не потолстевшие
  сабагенты. Записано в канон предупреждением: успех главного цикла иначе
  однажды прочтётся как регресс сабагентов.

  **Закрыто отказом там же:** длина имени сессии. В табе видно 8–9 знаков, и
  `C·детектор дрейфа канона` доезжает как `C·детек…`; предлагалось сузить задачу
  до одного слова — владелец отказал, полное имя читается в тултипе и списке
  сессий, обрезанный таб принят ценой. Пункт висел с 05.09 без решения.

---

- **v4.21: сторож имени смотрел на наличие, а позиция таба считалась
  недостижимой.** 06.09.2026. Две правки в `scripts/hand.sh`, обе — исправление
  ранее принятого решения, а не новая функция.

  **Первая.** Гейт брал строку `имя:` и передавал как есть; проверки формы не
  было. 05.09.2026 таб назвался `ссылка-в-Штабе` вместо `G·ссылка` — и скрипт в
  самом имени не виноват, ровно так и было вписано в шапку сессией, делавшей
  сплит. Дефект в том, что сторож охранял НАЛИЧИЕ строки, а конвенция говорит о
  ФОРМЕ. Теперь `hand.sh` отказывается кодом 4. Отказ, а не предупреждение —
  решение владельца: stderr тут уже проехал мимо глаз однажды (аварийная форма
  его печатала), а починка стоит одну строку в хендоффе, и соседние гейты
  (2 — несвежесть, 3 — чужая ветка) тоже отказывают кодом. Отсутствие строки
  оставлено предупреждением: отказывать на пустой шапке дороже, чем на кривой.

  **Что НЕ проверяется — тоже решение.** Карта букв живёт в `_capture.md`, и
  копия её в скрипте была бы вторым источником правды, который разъедется молча.
  Поэтому аварийная форма `E·feat/…` гейт проходит: форма верна, а то, что `E`
  не из карты, ловится глазом.

  **Вторая.** Вчерашний вердикт «рядом с родителем недостижимо в принципе»
  опровергнут: у Ghostty 1.3.1 есть словарь AppleScript с `select tab` и
  перечислением табов по именам. `window-new-tab-position = current` оказалась
  необходимой, но недостаточной — она ставит таб рядом с ВЫБРАННЫМ, а человек на
  родительском табе не сидит (замер: выбран таб 10, родитель — таб 5).

  **Адрес родителя не пришлось изобретать.** Преемник продолжает ту же задачу и
  понесёт то же `имя:`, а преемника ещё нет — значит единственный таб с этим
  именем и есть родитель. То, что канон уже требовал от шапки ради заголовка,
  оказалось и способом её найти. Четыре других опознавателя исключены замером:
  рабочий каталог у всех табов от `hand.sh` общий (временный каталог лаунчера),
  OSC-метку не послать (у Bash под агентом нет TTY), в дереве процессов имени
  нет (`claude --resume` идёт без `--name`), ключа конфига под поля заголовка у
  Ghostty нет (проверены все 453).

  **Оба сторожа укушены, и первый укус был бракованным.** Фикстура для гейта
  имени сперва не имела коммита, `git rev-parse --abbrev-ref HEAD` возвращал
  `HEAD`, и все шесть случаев одинаково падали на предыдущем гейте — «сторож
  краснеет» выглядело бы подтверждением, будь контроль только отрицательным.
  Спас положительный контроль: канонные имена обязаны были проехать, а не
  проехали. После починки фикстуры — `C·hand.sh`, `Л·лото`, `E·feat/…` → код 0;
  `ссылка-в-Штабе`, `AB·две-буквы`, `·без-буквы`, `C·` → код 4. Позицию кусали
  вживую: выбран нарочно таб 1, родитель — таб 9, новый встал 10-м; отрицательные
  контроли (0 совпадений и 7) фокус не двинули.

---

- **v4.20: дрейф канона не имел показания, и «перечитывай канон» было замкнутым
  кругом.** 05.09.2026. Замер v4.17 доказал, что идущая сессия живёт на снимке,
  а компакт его консервирует. Отсюда следовало жёсткое ограничение на форму
  лечения: правило, положенное в канон, до этих сессий не доедет — его же и не
  прочтут. Работает единственный класс — код, читающий диск сам, каждый ход, и
  несущий инструкцию в самой строке. Построен `hooks/canon-drift.py` на
  `UserPromptSubmit`.

  **Цену замерили ДО постройки, как и требовалось.** Опасение было, что греп по
  растущему транскрипту дорог: полный проход по самому большому файлу за историю
  (78 МБ) — 139 мс вместе со стартом питона, p99 (23 МБ) — 64 мс, медиана —
  31 мс, при таймауте соседнего хука 5 с. Возражение снято, и придуманные заранее
  обходы — кэш по mtime, чтение только хвоста — не понадобились и не сделаны.

  **Замысел пришлось поправить в двух местах, и оба вскрылись только замером.**

  Первое: сверка «версия снимка против версии диска» сработала бы на КАЖДОЙ
  сессии. Снимок несёт `SKILL.md`, а он объявляет v4.12 намеренно и постоянно;
  версия движков живёт в `CHANGELOG.md`. Номер версии для этой сверки негоден
  вовсе. Предикатов стало два: снимок сверяется СОДЕРЖИМЫМ, движки — ВРЕМЕНЕМ
  последнего касания.

  Второе — и оно едва не отменило конструкцию: компакт кладёт СТАРЫЙ снимок со
  СВЕЖЕЙ отметкой. Разводящий случай снят на боевом транскрипте `135ea3d2`:
  снимок взят 06.08 (v3.21), правка диска 12.08, последнее ПОЯВЛЕНИЕ снимка —
  компакт 17.08. Предикат по времени видит «правка старше появления» и молчит
  ровно там, где снимок протух сильнее всего. Поэтому снимок и сверяется
  содержимым.

  **Ретро-замер по 941 транскрипту** (диск реконструирован из git на момент
  каждого промпта): канон съели 185 сессий, детектор заговорил бы в 46 из них.
  Предикат по СОСТОЯНИЮ дал бы 750 строк на 3518 промптов (21%, до 108 строк на
  одну сессию); по СОБЫТИЮ — 68 (2%, максимум 4) при ТОМ ЖЕ наборе пойманных
  сессий. Различающая сила не потеряна, потерян только повтор, и выбрано событие:
  `_gates.md` называет цену прямо — тревога, которую промотали сто раз, перестаёт
  читаться.

  **Авторство правки не выясняется вовсе.** Отличить чтение от записи по имени
  инструмента нельзя: здесь канон правят через `Bash` не реже, чем через `Edit`,
  — первая попытка так и провалила положительный контроль. Вместо авторства —
  окно снисхождения в минуту. Слепое пятно в минуту ничего не стоит: дрейф
  меряется часами и днями.

  Стенд укуса 16/16, включая разводящую пару окна и контроль «замолчав, не
  глохнет навсегда». Поведенческая проба на свежих сабагентах: GREEN 3/3 —
  все три перечитали названный файл ПЕРВЫМ действием и вдобавок сверились с git,
  подтвердив, что коммит действительно приземлился. **Оговорка к самой пробе:
  RED показал промах 2 из 3, а не 3 из 3** — третий агент открыл `_gates.md` сам,
  собираясь коммитить. И первый заход RED был забракован целиком: сценарий
  «закрой заявку» ведёт в `_lane-c.md` сам по себе, там контроль промаха не
  показал ничего (тот же отказ, что в `2eb88fe`).

  Побочно и оплачено там же: `.gitignore` — белый список, `/hooks/` впущен
  каталогом, и `git add hooks/` утащил бы `__pycache__` в историю. Нашли три
  независимых пробы, подтвердил `git check-ignore`.

- **v4.18: имя сессии не принадлежало никому, а гейт свежести молчал.**
  05.09.2026. Человек показал таб-бар: `ХЕН…`, `Syst…`, `hand…` — «вот это
  ваще некрасиво». Два разных дефекта под одним симптомом.

  Первый — имени просто не существовало как поля. RED 3/3 на свежих контекстах:
  канон исполнялся безупречно (хендофф переносился на ключ по задаче, шапка с
  `ветка:`, обновление ДО запуска, `hand.sh -n` чисто) — и ни в одной пробе не
  появилось ни строки имени, ни `--name`. Преемник рождался как `✳ Claude Code`.

  Прежде чем чинить, замерили, кому вообще принадлежит заголовок терминала.
  Под pty: `claude` шлёт `ESC]0;✳ Claude Code` **на старте**, то есть затирает
  любой OSC-заголовок, поставленный лаунчером до него. Значит путь «`hand.sh`
  печатает escape-последовательность» мёртв по построению. Рабочим оказался
  флаг `--name` — с ним заголовок становится `✳ <имя>` (проверено там же).
  Отсюда конструкция: имя едет строкой `имя:` в шапке хендоффа, `hand.sh` её
  разбирает и пробрасывает, `/rename` руками не нужен.

  Конвенция `<буква проекта>·<задача>` продиктована замером, а не вкусом: в
  таб-баре видно около четырёх знаков, и два съедает префикс `✳ `, который
  Claude Code приписывает сам. Значит различает первый знак, а не смысл.

  GREEN 3/3. Оговорка к самому тесту: две пробы из трёх выдали имена, дословно
  совпавшие с примерами в тексте правила, — они подтверждают чтение, а не
  перенос. Разводящим был третий случай, `Л`=muzloto: букву из имени проекта не
  вывести, она только в таблице, а формулировку задачи скопировать неоткуда.
  Он прошёл — конвенция переносится.

  Второй дефект нашёл RED-агент попутно, и он дороже первого. **Гейт свежести
  `hand.sh` был инертен ровно там, где хендофф и лежит.** `git status
  --porcelain` схлопывает неотслеживаемый каталог в одну запись `?? .claude/`;
  проверка «это файл?» на ней ложна, «самая свежая правка» выходит пустой, и
  отказ кодом 2 не срабатывает, если изменены только файлы внутри такого
  каталога. То есть правило «несвежий хендофф хуже отсутствующего» держалось
  на проверке, которая молчала. Лечится `-uall`; проверено обоими концами —
  старым конвейером устаревший хендофф проходил, новым даёт код 2.

  Урок общий: **гейт, который никогда не отказывал, неотличим от исправного.**
  Отказ надо проверять его срабатыванием, а не отсутствием жалоб.

- **v4.17: идущая сессия живёт на снимке канона, а компакт его консервирует.**
  05.09.2026. Хендофф нёс подозрение, что правило, дописанное в скилл, не
  доезжает до уже идущих сессий. Замер по 893 транскриптам подтвердил и
  повернул вывод: скилл входит в сессию **ровно один раз** вызовом `Skill`
  (156 из 159 derflow-сессий), а компакт — единственное, что выглядело
  надеждой на обновление, — файл не перечитывает. Он переприкрепляет аттач
  `invoked_skills` с тем же текстом байт в байт.

  Доказал естественный опыт, а не рассуждение: сессия `135ea3d2` прожила с 07
  по 17 августа через четыре компакта на снимке **v4.0**, пока на диске уже
  стояли v4.1, v4.2 и v4.3. Независимо то же у `d58e819f`. Десять дней и три
  версии мимо, без следа в самой сессии.

  Практический вывод, который меняет способ править канон: **свежесть даёт не
  «скилл», а чтение файла в момент нужды.** Движки поэтому свежи (их читают),
  а `SKILL.md` — никогда. Меры отдельным решением; здесь только факт.

- **v4.16: «сплит» оставался меню, а закрытия с долгами не было вовсе.**
  05.09.2026. Человек показал прощальное сообщение чужой сессии — прозу «открой
  её в ворктри units-tracked-in-git и покажи хендофф» — и спросил: «Надо
  КОМАНДЫ. А в идеале, чтобы я говорил „сплит“, а новая сессия сама всё делала,
  зачем я должен выполнять обезьянью работу?»

  Две дыры, обе подтверждены пробами на свежих контекстах, 4 из 4.

  Первая — правило «каталог тот же → терминал не нужен вовсе → `/clear`».
  Оно отправляло на самый ручной путь **самый частый** случай (та же задача,
  тот же ворктри, раздулся только контекст) и стоило человеку двух действий:
  нажать `/clear` и вставить константу. Обе пробы сценария «сплит» пошли туда
  и меню даже не показали — то есть виноват был не абзац про три варианта, как
  казалось сначала. Одна проба вдобавок обосновала отказ от `hand.sh` ссылкой
  на `_parallel.md`: правило обрывало рассуждение раньше, чем агент доходил до
  оговорки, что окно двух сессий на дереве создаётся намеренно и коротко.
  Починка: «сплит» — одно исполнение (обнови хендофф → `hand.sh` → отчёт и
  молчание), совпадающий каталог не отменяет запуска, `/clear` только по прямой
  просьбе. v4.10 на ту же жалобу отвечала пометкой «что исполняешь ты, а что
  человек» — жалоба вернулась дословно через сутки. Пометка не лечит, лечит
  отсутствие выбора.

  Вторая — конца работы с отложенными долгами в каноне не было. Расщепление
  описано как продолжение работы, и правило прямо запрещает пускать через это
  меню что-либо ещё, — а здесь работа **останавливается**: приземлено то, что
  приземлилось, хвосты ждут человека, срока или друг друга, преемника нет.
  Агент проваливался в пустоту и сочинял прозу. Обе пробы этого сценария
  перечислили долги в чат сразу после фразы «состояние в хендоффе» (два
  расходящихся списка), не дали команды возврата и оставили долг со сроком без
  триггера — причём обе `schedule` **рассмотрели** и отвергли доводом «новую
  автоматику молча не завожу». Довод верный, вывод неверный: молчать нельзя не
  про заведение автоматики, а про её отсутствие. Починка: прощальное сообщение
  из трёх частей (что приземлилось · где лежит остальное · блок `cd` +
  `hand.sh`), вопрос человеку здесь законен в отличие от сплита (преемника
  нет — спросить больше негде), у долга со сроком обязан быть триггер или
  названный вслух отказ от него.

  Тем же заходом поправлена шапка `hand.sh`: она сама советовала `/clear` на
  совпадающем каталоге и после правки спорила бы с каноном.

  **Замер вдогонку, 05.09.2026: третьей дыры нет, не ищи её снова.** Человек
  показал сообщение сессии, у которой задача кончилась, а следующая работа —
  ДРУГАЯ заявка в другом ворктри: она сочинила стартовый промпт («Ворктри …,
  прочитай …, реализуй по `tasks.md`, начиная с секции 0») вместо константы и
  указала каталог прозой вместо `cd`. Выглядело четвёртым непокрытым случаем:
  сплит другую работу исключает явно, а `_parallel.md` про `hand.sh` не
  упоминает вовсе — «новая ветка И новая сессия» сказано, чем сессия рождается
  не сказано. Три пробы на свежих контекстах (289k/214k/176k, разные репозитории,
  одна дословно повторяла показанный случай) прошли **3 из 3**: агенты сами
  звали `hand.sh` на чужой ворктри первым аргументом, один заметил несовпадение
  автоподбора по ветке с именем файла и передал файл вторым, другой сделал сухой
  прогон `-n` перед боевым. Правка отменена по правилу `writing-skills`: контроль
  промаха не показал — писать нечего.

  Причина, по которой дыра закрылась сама: формулировка v4.16 «без аргументов
  берёт текущий каталог, иначе каталог первым аргументом, а на общей ветке ещё и
  файл вторым» перестала описывать `hand.sh` как инструмент расщепления и
  описала его как транспорт вообще. Обобщение выводится из неё без отдельного
  правила. Показанное сообщение — старый канон, а не открытый дефект.

  Исключённый тупик: чинить это ужесточением v4.15 («старый таб замолкает»)
  нельзя. Молчание законно ровно потому, что инициативу принял преемник; при
  закрытии преемника нет, и перенос правила туда запретил бы единственный
  момент, когда вопрос владельцу ещё можно задать.

- **v4.15: расщепил — и продолжил спрашивать в старом табе.** 05.09.2026.
  Сессия обновила хендофф, запустила `hand.sh`, дождалась таба — и следующим
  абзацем задала человеку вопрос о приоритете. Ответ уже лежал в хендоффе,
  который она сама и написала. «Уж передал всё в НОВУЮ сессию, в НОВОЙ и
  спрашивал бы. Это окно должно было закрыться.»

  Канон переносил СОСТОЯНИЕ и молчал про ИНИЦИАТИВУ. Отчёт о проделанном
  законен; вопрос — нет: человек читает его там, где ответить нечем, а сессия,
  которой ответ нужен, его не видит. Правка в хендофф после запуска тоже не
  спасает — новая сессия его уже прочитала. Признак нарушения механический:
  вопросительный знак в последней реплике старого таба.

- **v4.14: два довеска в движки, оба из одной денежной находки.** 05.09.2026,
  gmb_v2, находка 8.к20 фискальной линии абонемента.

  Первый — в `_gates.md`, к главе о радиусе фикса. Радиус меряли только по
  поверхностям: «добудь перечень вызовов грепом и закрой все». Здесь перечень
  был добыт правильно, обе двери закрыты — и этого не хватило, потому что
  дефектом было **умолчание** параметра, то есть тот же дефект, доступный
  вызовам, которых ещё нет. Радиус имеет и временну́ю ось. Поведенческий запрет
  умолчание не заменяет: добавленный «наличные + дистанционный канал» покрывает
  одно сочетание, а у двери с расчётом у стойки наличные законны, и её будущий
  пропуск он пропустит молча. Цена снятия умолчания — 15 правленых тестовых
  вызовов, разовая.

  Второй — в `_lane-c.md`, к правилу «severity куплена состоянием данных».
  Правило велело сходить и посмотреть; оно не говорило, как читать ОТВЕТ. Замер
  дал ноль пострадавших строк, и прямое прочтение — «не горит, отложим». Верное
  прочтение обратное: строк ноль потому, что путь только выкачен, а первым
  случаем была бы живая приёмка с необратимым фискальным чеком. Пустой замер на
  необратимом пути — довод чинить РАНЬШЕ, и окно на бесплатную починку ровно до
  первого случая. Правка уехала в прод перед приёмкой.

- **v4.12: расщепление плодило не окна, а ИНСТАНСЫ терминала.** 04.09.2026.
  Вскрылось от человека: «ты мне размножаешь окна, я работаю в одном терминале
  табами». Замер подтвердил — четыре живых процесса Ghostty, три с
  `--working-directory=`, то есть по одному на каждое прошлое расщепление.

  Корень не в настройке табов, и крутить её было бы напрасной работой.
  **`open -na` означает «новый ИНСТАНС приложения», а не «новое окно»**, а
  нативные табы macOS живут внутри одного процесса — инстансы не могут стать
  табами друг друга ни при какой конфигурации. Хуже того, это тот же корень,
  что у немоты транскрипта из v4.11: аргументы приходится склеивать в командную
  строку именно потому, что инстанс новый, и тем же движением наследуется
  окружение вызывающего. Один корень, два симптома — а v4.11 лечила симптом.

  Лечение: команда пишется во временный launcher, и работающему приложению
  отдаётся **файл**, а не аргументы — `open -a Ghostty.app <launcher>`, **без
  `-n`**. Ghostty объявляет `.sh` своим типом документа и по умолчанию
  открывает такое новым табом текущего окна (`macos-dock-drop-behavior =
  new-tab`). Отсутствие `-n` и есть вся починка.

  Побочно это закрывает v4.11 **по построению**: сессию порождает Ghostty, а не
  процесс Claude, поэтому `CLAUDE_CODE_*` до неё не доезжают вовсе. `unset`
  оставлен страховкой, но несущим быть перестал.

  Замерено сквозным прогоном на временном репозитории-фикстуре: инстанс не
  породился (три и три), файл ушёл в инстанс `1454` — основной, — хотя звали из
  `39549` (LaunchServices несёт к главному, а не к вызывающему: для уклада «одно
  окно, много табов» это верное поведение), транскрипт пишется, промпт-константа
  доехала. Исключённые тупики: `ghostty +new-window` на macOS отвечает `not
  supported on this platform`; System Events с `Cmd-T` работает, но требует
  Accessibility и печатает русский промпт клавишами.

  **Урок шире починки.** Правило v4.10 велело предлагать расщепление тремя
  вариантами, и первый обещал «новое окно» — формулировка была верна буквально и
  вредна на деле: она описывала, что делает вызов, а не что получает человек.
  Механизм, чью цену платит уклад работы человека, проверяется у человека, а не
  в замере.

- **v4.11: расщеплённая сессия не писала транскрипт, а ключ хендоффа
  вырождался ровно в `~/.claude`.** 04.09.2026. Оба вскрылись при попытке
  применить к себе правило расщепления, записанное часом раньше.

  **Транскрипта не было вовсе.** `open -na` передаёт окружение вызывающего
  процесса, а вызывающий — сессия Claude. Порождённая сессия наследовала
  `CLAUDE_CODE_CHILD_SESSION=1` и рождалась «дочерней»: запись транскрипта
  выключена целиком. Плюс чужие `SESSION_ID`, `BRIDGE_SESSION_ID` и
  `MESSAGING_SOCKET`/`TOKEN` — чужая личность и чужой канал. Цена не в
  неудобстве: **всё измерение расхода идёт по транскриптам**, значит каждая
  расщеплённая сессия выпадала из любого замера, и все цифры «сколько стоила
  работа» занижены на неизвестную величину. Инструмент ради экономии контекста
  делал невидимым собственный результат. `hand.sh` теперь глушит эти переменные
  в порождённой оболочке до запуска `claude`.

  **Ключ по ветке не ключует на стволе.** Канон ставил
  `.claude/handoff/<ветка>.md` вторым по уникальности — «уникален, пока
  держится ветка на проблему». На `main`/`master`/`develop` условие не держится
  по определению: там сидят все сессии разом. Больнее всего это бьёт в
  `~/.claude`, чей собственный `CLAUDE.md` объявляет шесть параллельных сессий
  и отсутствие изоляции ворктри, — то есть все на `main`, и `handoff/main.md`
  один на всех. Он ещё и в `.gitignore`: перезапись не оставляет ни конфликта,
  ни следа. (02.09.2026 туда писали через `cat >`, перезаписью целиком; повезло,
  что затирать было нечего.) Правило: на стволе хендофф ключуется ЗАДАЧЕЙ и
  передаётся явно; `hand.sh` на стволовом автоподборе предупреждает.

  **И меню сплита пометило, что исполняет агент, а что человек.** `/clear` —
  команда CLI, агент её выполнить не может; поставленные вровень варианты
  читались как равные, и человек резонно ждал полной автоматики. Первым теперь
  стоит тот, который исполняется целиком.

- **v4.10: потолок опущен до одного круга; критику дали механику на вход;
  расщепление перестало ждать просьбы человека.** 04.09.2026, тем же днём, что
  v4.9 — потому что v4.9 срезала кругам хвост, но не тронула ни цену круга, ни
  причину, по которой сессии живут по 24 часа.

  **Один круг вместо двух.** Аргумент за второй был один — замер «узкий проход
  бьёт широкий». Он никуда не делся, но относится к УЗОСТИ, а не к числу: теперь
  единственный круг и есть узкий, с поимённо перечисленными соседними модулями.
  Аргумент против второго сильнее: он критикует не заявку, а твоё сведение — то
  есть уже рекурсию. GREEN на потолке в один круг: 3 из 3 остановились и добрали
  нюанс, которого в тексте не было, — вскрытый механикой вычтенный сценарий это
  точечная правка и перегон механики, а не повод для круга.

  **`check.py` идёт критику ВХОДОМ.** Замер v4.9 показал, что круг стоит 460k, а
  промпт при этом уже узкий (медиана 2881 символ) — значит платится не за
  задание, а за ЧТЕНИЕ: критик минутами выясняет, существует ли символ, который
  скрипт разрешает мгновенно. Приём был доказан на свипе («механический проход
  первым, до фанаута» — 9 путей, 18 символов, 3 заявки) и к критик-слою не
  применён ни разу. Рычагов у цены круга ровно два — число кругов и механика на
  входе; ширина промпта рычагом не является.

  **`superpowers:brainstorming` встал ПЕРЕД `explore`.** Дыра, которую он
  закрывает, названа точно: свип сверяет код против спеки и **не может увидеть
  спеку, неверную о мире**. Оба самых дорогих улова первого круга были ровно
  этим — `design.md` сослался на механизм адресатов, которого не существует;
  круг тренеров брался у владельца ЮЛ, а у клубного владелец это общая сменная
  учётка без круга. Свип по такой спеке зелёный, фича сломана. Брейншторм
  проверяет замысел против человека и стоит разговора, а не 460k.

  **Расщепление: триггер тоже не должен быть человеком.** Канон v4.6 уже говорил
  «запускает агент, а не человек», но триггера не задавал — и человек всё равно
  просил команду каждый раз. Теперь признак машинный (`ctx:` из хука, порог
  150k, плюс закрытие заявки и смена проблемы), предложение — три исполнимых
  варианта одним словом, а хендофф обновляется ДО предложения, потому что
  `hand.sh` на несвежем откажет.

  **И две спотыкачки `hand.sh`, из-за которых автоподбор всегда мазал.** Поиск
  заявочного хендоффа брал `find | head -1` из шести сестринских — кандидат
  теперь обязан сам объявить текущую ветку, иначе перебор идёт дальше (проверка
  принадлежности стояла ПОСЛЕ выбора и превращала промах в отказ). Второй
  аргумент не был привязан к первому — теперь относительный путь считается от
  каталога, а хендофф вне каталога отвергается: промпт строится относительным
  путём и снаружи не разрешится.

- **v4.9: у критик-слоя не было потолка, и признак остановки был недостижим.**
  04.09.2026. Замер по транскриптам за 02–03.09: **45 прогонов критиков за два
  дня**, окно критики съедает **41 % output главного цикла** (1.25M из 3.04M), а
  на выходе из окна — **31 правка файлов** на 1024 ассистентских сообщения, то
  есть **≈40k токенов на одну приземлённую правку**. Промпты критикам при этом
  уже узкие (медиана 2881 символ, максимум 6597) — сужать было нечего.

  🔴 **Первая редакция этой записи приписала расход главному циклу — неверно.**
  Транскрипты сабагентов на диск не пишутся, поэтому замер по ним видел только
  половину. Вторую дал скриншот владельца: **один прогон `gap-finder` — 214.3k
  токенов и 8m29s**, столько же примерно у архитектора. Значит круг двух
  критиков — **400k+ в сабагентах**, плюс ~57k главного цикла на сведение и
  проверку находок: **≈460k за круг**. При 45 прогонах за два дня это **≈9M в
  сабагентах** против 1.25M измеренного главного цикла — критик-слой сжёг
  больше, чем весь main-loop output на всю работу обоих дней.

  🔑 **Узкий промпт не делает прогон дешёвым.** 2881 символ мишени — это не
  бюджет, а лицензия: агент читает столько, сколько сочтёт нужным. Единственный
  рычаг здесь — не ширина промпта, а ЧИСЛО прогонов.

  Причина не в цене, а в форме правила. Предикат «останавливайся, когда находки
  перестали ронять деньги, права и ПД» **недостижим по построению**: пока находок
  не ноль, а заявка денежная, он велит продолжать всегда. Плюс контур —
  каждое сведение пишет новый текст, новый текст никем не критикован, следующий
  круг находит дефекты в нём. Слой производил собственный повод для следующего
  круга, и это видно в его же таблице: **четыре кривой из семи немонотонны в
  хвосте** (15→11→11→**15**; 23→17→9→6→3→**4→5**; 26→9→8→**12**→6;
  14→8→**10**→7→6→5→4). Сходящийся процесс в хвосте не разворачивается.

  Хуже того, круги тратились не на ту ось — и это уже было записано в v4.6:
  семь проходов критиков на заявке 66/66 закрылись с нулём находок, а свип
  «код против спеки» следом нашёл **восемь** подтверждённых расхождений, из
  которых круги не увидели **ни одного**. Критики читают текст против текста;
  дефекты, доезжающие до прода, живут между текстом и кодом.

  RED-фаза (3 сабагента, свежий контекст, пять давлений: деньги, стоячее
  распоряжение владельца, 14 находок круга 2, девять из них — в собственном
  сведении, таблица замеров): **3 из 3 погнали круг 3 и запланировали ещё 2–4.**
  Все трое процитировали снятый предикат. Все трое сняли бюджетное возражение
  **переносом** круга в сабагентов — цена не останавливает, она перемещается.
  GREEN с потолком: 3 из 3 остановились, назвали механический проход и верно
  применили предикат замысла.

  Правило: **потолок два круга, слой закрывается СЧЁТОМ, а не признаком.**
  Сведение круга 2 проверяет механика (поимённый счёт сценариев, diff, числа в
  артефактах, `check.py`), а не третий критик. Бюджет уходит в свип. Законный
  выход за два круга — не круг: поехал замысел → `/opsx:update` или новая заявка.

- **v4.8: связи памяти проверяли глазами — шесть битых прошли мимо.**
  02.09.2026. Разбор памяти 01.09.2026 закончился строкой «целостность после
  правки чистая: битых указателей на память ноль». Написанный на следующий день
  `check.py` нашёл **шесть** — все из той же правки: 25 памятей промоутнули в
  движки и удалили, ссылки в соседях остались висеть.

  🔑 **Утверждение о целостности — тоже утверждение о коде, и своё подозрительнее
  чужого.** Ручная сверка 65 файлов выглядит выполнимой, потому и была сделана
  руками; выполнимой она не оказалась.

  Инструмент — отчёт, не гейт: код возврата всегда 0, файла подавлений нет,
  ненулевой выход не заводили (решение владельца). Ложная тревога каждый запуск
  превращает гейт в то, что проматывают, а читателя-человека десяток строк
  чужого словаря не останавливает.

  ⚠️ **Несущее решение — markdown не входит в индекс идентификаторов.** Пустив
  его туда, получаешь спеку, которая удовлетворяет сама себя: символ находится в
  её же тексте. Проверено мутацией — с `.md` в индексе чекер теряет ровно
  `PromotionService`, то есть настоящую находку.

  Замер живого прогона вскрыл и собственную регрессию: признак пути `"/" in tok`
  засосал URL-маршруты и команды оболочки — 326 находок вместо 13. Путь обязан
  нести расширение и не начинаться со слэша.

- **v4.7: правило велело стирать неверную память — вместе с фактом уходил урок.**
  02.09.2026, разбор внешнего инструмента (**memora**, MCP-память с цепочками
  версий и извлечением по требованию). Инструмент не внедрён — он чинит
  **доставку**, тогда как у нас ломалась **сортировка**, и он снижает трение
  записи, то есть усиливает ровно то, что породило рост. Но две его конструкции
  вскрыли дыры в нашем правиле:

  🔑 **Оказавшаяся ложной запись несёт два факта, а стирание уносит оба.** Сам
  факт и то, что мы верили в обратное. Второй часто дороже: он объясняет, почему
  следующий пойдёт в тот же тупик. Отсюда `supersedes:` вместо удаления — тело
  остаётся находимым, из индекса уходит, урок сохраняется.

  🔑 **У памяти нет срока, и это единственная причина, по которой растёт именно
  она.** Спеку заменяют, заявку архивируют, а память просто остаётся. Поле
  `живёт до:` чинит это дешевле любого механизма. Возвращаться к memora, если
  под границей хранилищ память всё равно начнёт пухнуть: это будет
  доказательством, что дисциплиной она не держится.

  Оговорка: стоимость 43 инструментов MCP в контексте не измерена. После того как
  индекс gmb-v2 ушёл с 50.7 КБ до 11.6 КБ, поверхность инструментов вполне может
  стоить дороже того индекса, ради которого её заводят. Мерить `context-spend.py`
  до внедрения, а не после.

- **v4.6: расщепление сессии оплачивал человек, а канон имени воспроизвёл
  общий изменяемый ресурс.** 01.09.2026. Правило v4.4 велит расщеплять сессию, и
  чем лучше оно работает, тем чаще ритуал: владелец просил «дай команду для новой
  сессии», агент **на пике контекста** сочинял её и заново пересказывал
  состояние, владелец руками переносил два куска в новый терминал. Промпт при
  этом дублировал `HANDOFF.md` — состояние описывалось дважды и расходилось.

  🔑 **Транспорт между сессиями не должен быть человеком, а промпт не должен
  сочиняться.** Состояние принадлежит файлу; как только имя предсказуемо, промпт
  становится константой, и сочинять на самом дорогом контексте больше нечего.
  Отсюда `scripts/hand.sh` (запускает **агент**), `/clear` вместо нового
  терминала, когда каталог не меняется, и отказ по несвежему хендоффу — боевое:
  сессия предлагала расщепление, имея в файле строку «картинка не проверялась»,
  неверную уже несколько часов.

  🔑 **Канонизация имени и есть то, что создаёт столкновение.** Первая редакция
  правила говорила «`HANDOFF.md` в корне репозитория» — то есть общий изменяемый
  ресурс, корень из `_parallel.md`, воспроизведённый заново. Поймано вопросом
  владельца: «а если две сессии одновременно попросят расщепиться?». Две сессии в
  одном дереве затирают хендоффы друг друга, и преемник **уверенно продолжает
  чужую работу** — хуже потери, потому что молча. Путь ключуется задачей
  (заявка → ветка → корень для совместимости), хендофф объявляет `ветка:`,
  `hand.sh` отказывается кодом 3 при несовпадении. Столкновение возможно только
  при уже нарушенной изоляции — проверка не чинит нарушение, она делает его
  громким.

  Побочно: три ошибки одного семейства в bash 3.2 (системный в macOS) — `${var@Q}`
  отсутствует, `printf %q` разносит UTF-8 по байтам, а без установленной локали
  многобайтные `«»` затягиваются в имя переменной, так что `$var»` становится
  несуществующим именем. Все три нашлись прогоном, ни одна — чтением.

- **v4.5: память проекта разрослась вдвое за свой потолок, потому что правила,
  что куда класть, не существовало.** 01.09.2026, gmb-v2. Владелец: «я был
  уверен, что docs-first — поэтому у нас есть спеки; как что работает, мы чекаем
  спеки. Это и есть память. Либо я что-то не так понимаю».

  Понимал он верно. Не хватало не доктрины, а **границы**: в derflow был раздел
  «что openspec НЕ держит», но он про маркетинг, и про память там не было ни
  слова. Знание оседало там, где агент стоял в момент, когда его узнал.

  **Замер на 01.09.2026:** 104 спеки, 209 архивных заявок, 98 в работе, 191
  память. Индекс `MEMORY.md` — 50.5 КБ и 191 строка при пределах **25 КБ и 200
  строк**, то есть вдвое за байтовым потолком: половина памяти не доезжает
  молча. Вместе с проектным `CLAUDE.md` на 28.7 КБ это ≈26k токенов до первого
  хода; при 44 924 ходах за неделю — ≈$1.7k только на два всегда-загруженных
  файла. В индексе при этом 6 хендоффов, 26 строк про закрытое и выкаченное, а
  медиана строки 261 символ при правиле «одна короткая строка».

  **Промежуточная гипотеза была проверена и опровергнута.** Я предположил, что
  память переполнилась как перелив забитого конвейера openspec: 98 открытых
  заявок выглядели спек-долгом. `openspec list --json` показал, что полностью
  выполненных и незаархивированных — **5**. Конвейер исправен. Ломается
  сортировка, а не пропускная способность, и правило пришлось строить на этом, а
  не на красивом объяснении.

  🔑 **Спека отвечает ровно на один вопрос: что ДОЛЖНО быть истинно.** Она
  нормативна и потому не хранит собственных нарушений, формы реализации,
  обманчивых имён, состояния данных и уроков о том, как работаем мы.
  Спека = что должно, память = что есть и где ошибётся следующий. Память не
  вторая спека, а errata и карта ловушек — и именно поэтому при работающем
  docs-first её должно быть **мало**.

  🔑 **Всегда-загруженное — привилегия, а не место по умолчанию.** Индекс памяти
  и проектный `CLAUDE.md` оплачиваются каждым ходом навсегда, поэтому туда
  попадает только то, что нужно знать ДО того, как известна задача. Ловушка про
  конкретный флаг этому не удовлетворяет.

  🔑 **Растёт именно память, потому что она единственная без срока и владельца.**
  У спеки есть замена, у заявки — архивация. У памяти нет ни того, ни другого, и
  при этом она всегда загружена. Отсюда следует, что уборкой это не лечится:
  уборка покупает недели, границу — нет.

  Побочно вскрыт незакрытый дефект: каталог памяти привязан к слагу рабочего
  каталога, поэтому ворктри своей памяти не имеет и основную не видит — а
  `_parallel.md` предписывает вырезать ворктри до первой правки. Знание проекта
  недоступно ровно там, где идёт работа. Механизм сшивки не выбран; дефект
  назван в `_capture.md`, чтобы пустая память не читалась как отсутствие ловушек.

- **v4.4: неделя выгорала на контексте, а подозревали критик-слой и сабагентов.**
  01.09.2026, все проекты. К полудню понедельника съедено 75% недельного лимита,
  который гасился только в четверг. Рабочая гипотеза владельца: черновую работу
  надо унести на внешнюю модель, оставив Claude только постановку и проверку.
  Моя первая гипотеза была не лучше: жжёт критик-слой — два субагента по ~100k на
  одну узкую правку спеки выглядели очевидным виновником.

  **Обе гипотезы оказались неверны, и разошлись с фактами на порядок.** Замер по
  локальным транскриптам за неделю (66 965 ходов):

  - cache-read — **76%** расхода, cache-write — 16%, output — **7%**;
  - главный цикл — **90%** output и **95%** cache-read; сабагенты — 9% и 5%;
  - медиана контекста на ход главного цикла — **486k**, p90 — 862k, p99 — 977k,
    то есть каждый десятый ход упирался в потолок окна 1M;
  - 51 435 ходов главного цикла за неделю; одна сессия на 3 638 ходов дала 1.9B
    cache-read ≈ $3.5k по прайсу API, десять самых длинных — 43% всего
    cache-read; неделя целиком ≈ $52k в пересчёте на прайс.

  Планируемая правка — унести черновую работу из сабагентов — целилась в **4%**
  счёта. Подозреваемый критик-слой был шумом на фоне 25 миллиардов cache-read
  главного цикла: два прогона по 100k это 0.0008 недели.

  🔑 **Расход сессии квадратичен по её длине, а не линеен.** Каждый ход
  перечитывает весь накопленный контекст, поэтому одно чтение файла оплачивается
  не один раз, а на каждом последующем ходу до конца сессии. Сумма по сессии
  длиной n растёт как n² — и расщепление одной длинной сессии на k коротких
  делит счёт примерно на k, не отнимая от работы ничего. Отсюда же следует, что
  окно 1M не экономит, а снимает предохранитель: на 200k компакция режет
  контекст принудительно, на 1M медиану ничто не держит.

  🔑 **Дорогое место контринтуитивно, поэтому его нельзя выбирать гипотезой.**
  Оба independent предположения — владельца и моё — указали не туда, и оба звучали
  правдоподобно. Отсюда инструмент, а не правило: `scripts/context-spend.py`
  (`--days`, `--sessions`) считает структуру расхода по транскриптам за минуту.

  **Правка.** Экономика — в `_capture.md` (владелец границы контекста): что
  оптимизировать и что нет, «в контекст попало — значит множится», отчёт
  сабагента живёт до конца сессии, 1M под названную нужду. Триггер — в
  `_parallel.md`: тот же вопрос «та же проблема или новая?», который уже
  перерешал ветку, теперь перерешает и **сессию**; передача файлом, а не
  прокруткой.

  **И то же лечение, что у параллельности: предикат, а не память.** Агент не
  видит размер собственного контекста — ему нечего вспоминать. Поэтому
  `hooks/context-meter.py` печатает `ctx: <N>k` каждым ходом рядом с веткой
  (`UserPromptSubmit`), с порогом `‼` на 300k. Правило без наблюдаемого предиката
  дрейфует — это ровно вывод v3.9 про ворктри, применённый второй раз.

- **v4.3: критик-слой закрыт первым проходом, а спека после него выросла на две
  трети.** 13.08.2026, GM+, заявка `widget-venue-self-serve`. Правило «гоняй на
  свежей спеке» выполнено буквально: критики прочли редакцию, свежую **на момент
  запуска**. Затем в неё влились 38 резолюций, добавились два целых требования и
  четыре решения владельца, требование о tenant-изоляции переписалось почти
  полностью — и в таком виде текст не читал никто. Слой считался пройденным.

  Вскрылось вопросом владельца: «а ты критик-слой прогнал по готовой спеке?»

  **Второй проход нашёл пять вещей, четыре из них внесены самим автором при
  слиянии:**

  - резолюция «запретить кеширование бандла» приземлена в приложение, тогда как
    на проде файл отдаёт nginx и до приложения запрос не доходит — задача
    выглядела бы выполненной и не меняла ничего, а на ней держался весь
    переставленный порядок выкатки;
  - новая capability добавлялась только в карту кода, которая на засеянной
    таблице прав не действует: тесты идут по пустой таблице и были бы зелёными, а
    экран не открылся бы ни у одного носителя ступени — смысл заявки отменился бы
    молча;
  - требование «таймаут и повтор», добавленное ПРИ СЛИЯНИИ ради удобства,
    открывало двойную бронь с двумя платёжными ссылками: ключ идемпотентности
    пишется после коммита, и повтор «в полёте» проходит мимо него;
  - SHALL «правила самоброни применяются без исключений» был невыполним: из
    четырёх перечисленных правил одно не энфорсится нигде, другое относится к
    функции, вынесенной из заявки по non-goal;
  - «отсутствие `Origin` — несовпадение» отключало собственную страницу
    встраивания, потому что браузер не шлёт этот заголовок на same-origin GET.

  🔑 **Слияние резолюций — отдельный источник дефектов, а не механическая
  операция.** Первый проход слеп к тому, что появилось после него: он критиковал
  текст, которого больше нет. И самое дорогое приходит не из пропущенной критики,
  а из правок, сделанных ПРИ сведении, — их не видел вообще никто, включая
  автора, который перечитывал их как «уже согласованное».

  Поэтому правило не «помни про второй проход», а условие завершения: слой
  закрыт, когда проход по итоговой редакции не породил правок. Форма выбрана
  структурной намеренно — провал был не «забыл», а «счёл многошаговый гейт
  закрытым после первого шага», и напоминанием такое не лечится.

- **v4.2: `--yes` на архивации снимает единственный механический барьер.**
  09.08.2026, GM+. Заявка `booking-group-payment-link` заархивирована, дельта
  влита в живую спеку как истина, и **три сценария из четырёх не работали
  никогда**: экран результата выпуска платёжной ссылки не видел ни один человек
  ни разу за всё время жизни фичи — блок сам себя перечитывал после выпуска и
  подменялся заглушкой загрузки, уничтожая только что показанный результат.
  Вскрылось жалобой менеджера «вы не сделали то, что я просил»; он оказался прав
  против спеки, тестов и ревью одновременно.

  **Дважды ошибся в диагнозе, и обе ошибки поучительнее самого правила.**

  Первый диагноз: «архивируют, не проверив, — нужен conformance-sweep». Неверно
  дважды. Sweep читает код и потому слеп к двум дефектам из трёх: код был
  безупречен и просто не доезжал до экрана (родитель сносил поддерево, загрузчик
  обнулял выбор). И главное — проверять никто не забывал.

  Второй диагноз: «намерения проверять не было, надо велеть смотреть». Тоже
  неверно, и это выяснилось только потому, что владелец спросил «ты уверен, что
  проверок не было?». Замер вместо утверждения: **101 заявка из 162 записывает
  визуальную проверку, 154 — тесты.** В самой упавшей заявке визуальная проверка
  была записана задачей 6.6 и осталась **единственной неотмеченной из тридцати
  четырёх**. Намерение было и было правильным.

  Настоящая причина механическая: CLI считает незакрытые задачи и **блокирует по
  умолчанию** (`archive.js`, `incompleteTasks > 0` → подтверждение с
  `default: false`). Снимает блок ровно флаг `--yes`, который агент ставит
  рефлекторно, чтобы не повиснуть на интерактивном промпте, — и вместе с промптом
  сносит проверку. Масштаб: **87 архивов из 162 содержат неотмеченные задачи.**

  Поэтому правило не «помни проверить» (помнили), а «не снимай предохранитель»:
  `--yes` только при нуле незакрытых задач. Таблица «род утверждения → чем
  подтверждается» осталась вторым слоем — отметку ставит тот же, кто работал, —
  но несущее здесь механическое.

  **Третья ошибка — и её поймал уже тест.** Первая редакция правила несла таблицу
  «чем подтверждается сценарий», таблицу отговорок и красные флаги. Контроль это
  не подтвердил: три субагента **без** этого файла, под давлением владельца («я
  проверил сам», «промптов не жди»), при 9/28 и при 22/25 задач — **отказались
  архивировать все трое**, а один полез в БД прода и уличил владельца во лжи.
  Суждение у агента не сломано; писать ему «помни проверить» — грузить контекст
  тем, что он и так делает. Всё это вырезано.

  Воспроизвёлся ровно **рефлекс флага**: `--yes`/`-y` написали **3 из 3**, в том
  числе те, кто тут же отказался архивировать. Правило оставлено одно и узкое.

  Побочно тест нашёл изъян в самом правиле: «любой `- [ ]` блокирует» сработало
  бы на задаче «заархивировать заявку» (`unified-money-statement`), которую
  нельзя выполнить иначе как архивацией. Считаются работа и проверка, не ритуал.

  Уроки шире правила, оба оплаченные:
  1. **Прежде чем чинить дисциплину документом, замерь, была ли дисциплина.** Два
     раунда правки ушли в молоко на непроверенном «наверное, не проверяли».
  2. **Контроль без правила обязателен, и лёгкий сценарий его подделывает.**
     Первые два прогона давали 9/28 — отказаться там не требует дисциплины.
     Исторический провал был 33/34 с единственной незакрытой проверкой, и тест
     обязан повторять именно эту форму, иначе меряет не то.

- **v4.1: `_ship.md` разделён по триггеру — изоляция уехала в `_parallel.md`.**
  Файл обслуживал три разные аудитории под одной шапкой: инцидент F, полосу Ops и
  «всегда перед первой правкой в репо, который работается параллельно». Третий
  триггер срабатывает почти на каждой задаче, а нужны ей были ворктри, жатва и
  «новый запрос = новая ветка» — около четверти файла. Остальные три четверти
  (инцидентный путь, Ops-гейт, `land.sh`, срок годности ветки) грузились впустую.
  Замер: полоса B в параллельном репо 9 429 → 7 483 токена (−21%), C 14 002 →
  11 649 (−17%), Ops 8 480 → 7 199 (−15%). **Обратная сторона, названная явно:**
  инцидент в параллельном репо теперь грузит оба файла и стоит 10 368 против
  9 429 (+10%) — размен самой редкой полосы на самую частую. Ни одно правило не
  переписано, перенос дословный; аудит по заголовкам и строкам ошибок: 118 → 119
  пунктов, потеряно ноль.

- **v4.1: внешне-контрактный гейт переехал из `_lane-c.md` в `_gates.md`.**
  Правило начиналось словами «(любая полоса — B тоже)», но лежало в движке Lane C,
  который полоса B никогда не открывает. То есть задача B с внешним API своего же
  гейта не видела — ровно тот класс промахов, ради которых он написан (KkmServer,
  СБП). Критерий обязан быть там, где решают: `_gates.md` читает любая полоса,
  отгружающая код.

- **v4.1: правило гигиены контекста отменено — контроль не воспроизвёл провал.**
  Написано было под наблюдение «вывод Bash съел 240k, четверть окна». Замер по
  методике `writing-skills` (5 повторов на плечо): контрольная группа **без**
  правила ни разу не прочитала файл целиком — 0 из 5, все сами начинали с
  `wc -l`/`grep -c`. Провала, ради которого писалось правило, в контроле нет,
  значит чинить нечего. Хуже: пункт «тяжёлое чтение — сабагенту» дал прямой вред —
  два повтора из пяти упёрлись в делегатов, которые ничего не вернули, и **один
  сочинил содержание файлов**, признавшись только на самопроверке. Правило
  предписывало действие, которого среда не обеспечивает, и не говорило, что
  делать при отказе. Откачено целиком.

- **v4.1: расход контекста измерен, а не угадан — чинить нечем.** После отката
  проверили гипотезу на данных: 34 транскрипта, 2 358 вызовов Bash. Монстров нет —
  средний вывод 179 токенов, самый большой за всю историю 4 624, вызовов тяжелее
  5k ноль. Топ-10 самых жирных дают 5.5% объёма; 72% лежит в полосе 100–1000
  токенов, размазанной по тысяче обычных команд. Потолок на вывод (кандидат в хук)
  при 2 000 токенов экономит 1.1%, при 200 — 44%, но обрезает 644 вызова, включая
  прогоны тестов и логи. И около двух третей объёма пришло с команд, где
  ограничитель (`| head`, `--stat`, `wc`) **уже стоял**. Вывод: объём делает не
  размер вызова, а их количество; ни хук, ни правило этого не меняют. **Урок
  метода: сначала измерь провал, потом пиши правило** — здесь он был написан
  дважды подряд по догадке.

- **v4.1: версия в `SKILL.md` отстала на мажор.** «Deliverable evolves» держал
  v3.21, когда changelog и заголовки разделов уже были на v4.0. Номер в живом
  документе — то, чем сверяют, тот ли файл читаешь.

- **v4.0: derflow перестаёт пересказывать openspec.** Конфиг был написан под
  OpenSpec 1.5.0; к моменту разбора CLI ушёл на 1.8.0 и вырастил `/opsx:verify`,
  `/opsx:bulk-archive`, `/opsx:onboard`, `/opsx:update` — команды, дублирующие
  или обесценивающие значительную часть `_lane-c.md`. Правило: **derflow не
  держит ничего, во что openspec может вырасти.** Граница проходит по «внутри
  заявки / вне заявки», и апстрим её сам подтверждает — в `docs/workflows.md`
  прямо перечислено, что не покрыто: bugs/hotfixes/incidents, нужен ли малому
  изменению полный proposal, заслуживает ли задача change вообще. Это дословно
  работа роутера.

- **v4.0: триаж «сломалось» — до выбора полосы.** Прод-косяк включал F и Dx
  одновременно, и роутер не говорил, какая побеждает, а ведут они в
  противоположные стороны. Хуже: один симптом бывает пробелом модели (C),
  забытым рендером (B) или багом (F/Dx). Три вопроса от дешёвого к дорогому,
  потолок — три вопроса и одно чтение файла. (Боевое: «тренер сделал серию, а
  ссылки нет» — если у серии нет адреса в модели, хотфикс чинит симптом там, где
  нужна была фича.)

- **v4.0: граница B/C переехала в роутер.** Проверяемая таблица лежала в
  `openspec-rules`, а решение принималось в `SKILL.md`, куда она на этот момент
  не загружена. Критерий обязан быть там, где решают.

- **v4.0: редкие полосы во второй уровень.** Главная таблица ужата с 16 строк до
  6. C-bootstrap, C-adopt, C-drain, D, E, Spike, Orient и Docs срабатывают
  редко, а внимание при классификации стоили каждый раз.

- **v4.0: Docs — не полоса.** «Доки = источник правды» противоречило
  центральному тезису, что источник правды один и это `openspec/specs`. Похоже,
  полоса не работала именно поэтому. Разведено по цели: точечные доки → B без
  специалиста; «синхронизировать доки с кодом» → C-drain.

- **v4.0: терминус brainstorming больше не подавляется — он исчез.** Пятнадцать
  строк в `_lane-c.md` существовали только чтобы `superpowers:brainstorming` в
  конце длинного брейншторма не увёл дизайн в `docs/superpowers/specs` и не
  позвал `writing-plans`. Правило, вся работа которого — гасить поведение
  другого движка, есть признак интеграции, которой не должно было быть. С
  нативным `/opsx:explore` чужому терминусу неоткуда взяться.

- **v4.0: reap-гейт требует CLI, ручной `mv` запрещён.** `openspec archive` по
  умолчанию вливает дельту в спеки; пропуск требует явного `--skip-specs` с
  названными законными случаями. Генератный `openspec-archive-change` обходит
  CLI целиком — `mkdir` + `mv`, а синхронизацию выносит в необязательное
  приглашение с «Archive without syncing» как равноправным вариантом и
  guardrail «don't block archive on warnings». Безопасный дефолт инструмента
  подменён вопросом. Править навык бесполезно: `openspec update` перезапишет.

- **v4.0: два правила из схемы CLI, без которых архивация теряет данные молча.**
  `## Purpose` обязателен в дельте новой capability (иначе `TBD`-заглушка;
  чинится только прямой правкой спеки — схема запрещает дельтой). `MODIFIED`
  обязан быть полным блоком; частичный теряет сценарии при слиянии через CLI, а
  генератный `openspec-sync-specs` советует обратное — и это верно только для
  агентского слияния. (Замер: 17 из 24 спек в mprz.ru остались с `TBD`.)

- **v4.0: C-adopt снят, модель апстрима.** Было: археология кода → `descriptive`
  SHALL'ы → промоушен в `normative` → sweep → baseline. Стало, дословно по
  апстриму: *«You do not document your whole codebase to start. You write specs
  only for what you're about to change.»* Основание — замер: теги
  `normative`/`descriptive` нашлись в 2 файлах спек из 24, то есть работа шла по
  упрощённой модели и без слоя. Навык `adopt-code` не впущен.

- **v4.0: C-drain делегирован `/opsx:bulk-archive`** — он архивирует пачкой и сам
  детектит конфликты спек. Своим остался порядок вытеснения из git: активные ID
  бездатные by design, по имени их не упорядочить.

- **v4.0: третья общая поверхность — ветка интеграции.** Глава `_ship.md`
  объявляла две (дерево и деплой-таргет). Между ними лежит приземление, и там
  своя патология: «прогнал сюиту → кто-то влил своё → зелёный протух → гоняю
  заново», при 8–12 параллельных сессиях цикл может не сойтись. Лечение —
  замок на «ребейз → прогон → мерж», затронутые тесты под замком, право проезда
  у инцидента, одна точка интеграции, срок годности ветки,
  `delete-branch-on-merge` как предохранитель.
  (Замеры на GM+: 171 мёртвая ветка, полностью влитая в `main`;
  `feature/location-window` — один коммит своей работы при отставании на 461;
  `feature/pd-operator-switch-ooo` — шесть при отставании на 1006.)

- **v4.0: деплой-таргет объявляется в проектном `CLAUDE.md`.** `develop`
  совпадал с `main` до коммита и выглядел рудиментом — а с него деплоился
  `demo.gmplus.ru`. Предложение удалить ветку остановил человек, потому что
  связи «ветка → окружение» не было в репозитории нигде. Агент вывести её не
  может.

- **v4.0: агенты — пять, отбор по счётчикам вызовов.** `system-architect` 110,
  `ui-ux-designer` 108, `frontend-developer` 107, `gap-finder` 85, `python-pro`
  45; у шести остальных ноль. Считалось по `subagent_type` в логах сессий —
  первая попытка мерить упоминания в файлах и коммитах дала неверный ответ по
  трём агентам, потому что мерила документацию, а не использование.
  `backend-architect` (5) и `code-reviewer` (16) отложены.

- **v3: External-integration gate.** Get the real wire-contract (vendor ref or
  captured sample) before writing a parser/serializer; "verify-on-live" is not a
  substitute (KkmServer incident 2026-07). Critique/`code-review` check +
  `gap-finder` lens for a missing external response schema.

- **v3.1: openspec bootstrap, not abandon.** `openspec/` absent → `openspec init`
  (not a fallback to `docs/superpowers/specs`, which are dated design *history*, not
  a co-living spec). New subsystem → author in openspec directly. Existing unspecced
  seam → `adopt-code` warm-started from a design doc (intent head-start, verified vs
  code), then a supersede-pointer on the old doc. `audit-code` = optional drain
  accelerator, not an entry ticket.

- **v3.2: Project/context boundary — findings, not context.** Work happens in the
  project whose context it needs; across a boundary only the distillate travels.
  The meta-repo receives lessons, not product work. A cross-project skill runs in the
  project holding the material; its learned rules write back to the global store.

- **v3.3: openspec init timing + marketing≠SHALLs.** `init` lands at explore→propose
  (brainstorm-first without init is correct, not "behind"). Content/marketing-heavy
  work has two homes: openspec holds *behavior/contract* only; positioning/offer/copy
  live in marketing homes (`product-marketing.md`, offer/copy docs), never as SHALLs.

- **v3.4: External-gate binds claims, not just parsers.** Any statement that an
  external API can't do X / doesn't return Y — in design, architecture, or review —
  must come from the vendor doc or a captured sample, never from our own code (the
  parser shows what we extract, not what the API sends). Nearly cut a valid
  phone-match login: `system-architect` inferred "no payer phone" from our parser;
  the vendor doc had `payerMobileNumber`.

- **v3.5: Handing verify to the human is not verify.** Floor = one successful run of
  what you built, by you, before "done". "No auth" is not an exemption (test-client
  force-login catches a 500-on-load); genuinely unreachable contour → declare "NOT
  verified + risk", never "готово, проверяй". (GMB "Продажи" shipped as done → HTTP
  500 on first load.)

- **v3.6: UI placement is design, not free build.** Adding an element to a
  constrained surface (modal/toolbar/card) → propose 2-3 / consult `ui-ux-designer` /
  ask where it goes, don't cram silently; then visual-verify the render (a new
  scrollbar/overflow is a regression). Verify gate: for UI, the contour is *visual*.
  (GMB modal: a "ЮЛ не задана" line crammed in → a scrollbar appeared, unproposed.)

- **v3.7: A missing UI element can be a missing domain relationship.** "X isn't
  shown / who is X?" → first ask whether the model expresses X and what it derives
  from (`gap-finder` on the data), before drawing a field — else you ship a tidy
  field that still doesn't answer the question. ("орг я, а ЮЛ-то кто?" = no
  organizer→ЮЛ path, not a missing input.)

- **v3.8: Announce-right, then drift.** A delegated skill's terminus can override
  derflow's *after* a correct announce. `superpowers:brainstorming` (borrowed for
  Lane C explore) ends its checklist at "design doc → `docs/superpowers/specs/` →
  `writing-plans`" — that terminus reasserts at the end of a long brainstorm and
  sinks the openspec plan. Borrow its technique, **void its terminus**: spec →
  `openspec-propose`, no `writing-plans`; restate at the explore→propose handoff.
  (Live: a fresh session announced openspec perfectly — init-at-propose, SHALL in
  openspec — then finished into `docs/superpowers/specs` + writing-plans.)

- **v3.9: Parallel isolation is structural + by-default, not agent-noticed.** A
  session is blind to its siblings — so when a repo is worked in parallel, every
  file-editing task carves its own worktree+branch *before* the first edit (isolation
  replaces coordination — git's 1000-dev model). The declared fact lives in the
  project `CLAUDE.md` (always-loaded, survives drift); the agent can't self-detect
  parallelism. Inherent limit: same-line edits conflict at merge (sequence + resolve).

- **v3.10: "Аудит" is a false friend + audit-code costs by design.** "Аудит" meaning
  "orient me so I can change area X" is **scoped `explore-code`** on that seam, not
  the whole-repo `audit-code` — which is a Workflow fan-out (one worker per hotspot),
  expensive *by design*. And `audit-code` itself now **caps N (≈10–15) and estimates
  tokens before fanning out** (a `+Nk` directive is a hard `budget.total` ceiling) —
  "top-N of the hot 20%" without a cap is 50+ workers. (Live: "аудит код" to prep a
  tournament feature → 55-worker Workflow ≈ 2.9M tokens; the ask was scoped explore.)

- **v3.11: Resolutions land in the normative spec, not design-prose.** When a critique
  returns numbered resolutions (N1, N2…), verify each is a **SHALL in `spec.md`**, not
  just prose in `design.md` — the normative spec governs and `_conformance-sweep`
  enforces it; a fix that lives only in design quietly reopens at implementation. Map
  each N → SHALL before accepting "closed." (Live: architect's N2/N3/N5/N8/N11/N12 on
  the tournament spec were verified *in the SHALLs*, not on the summary's word.)

- **v3.12: Isolation covers the git tree, not the deploy target.** Worktree-per-problem
  (v3.9) stops working-tree collisions, but the running server (built `dist/`, artifact on
  the box) is a *second* shared mutable surface isolation never reaches — two sessions build
  from different branches and the last push wins. Deploy = a serialized op on shared state:
  **build only from merged `main`, never deploy an unmerged branch to shared prod, serialize
  deploys, verify the active served chain (index→chunk→marker) + re-check after a delay.**
  (Live: a merged frontend deploy was overwritten 15 min later by a parallel session building
  from its unmerged branch — backend intact, buttons gone; the clobbering build wasn't from main.)

- **v3.13: A new request mid-session is a new problem — re-decide the branch.** v3.9 fires at
  the start; the trap is mid-session — on feature-branch X the human points at a nearby bug and
  the agent fixes it *in X*, polluting the feature (can't ship alone). Before the first edit of
  any new request: `git branch --show-current` + "same problem or new?"; new → branch from `main`,
  not from X. The agent can't self-detect, so branch + dirty state is surfaced every turn via a
  `UserPromptSubmit` hook (settings.json). (From a live recurring miss: nearby-bug fixes kept
  landing in the active feature branch.)

- **v3.14: "Explore" is overloaded — Lane C explore ≠ Orient explore-code, and never run both
  over the same files.** Lane C *explore* is design-thinking seeded by the living spec: read
  `openspec/specs` first (the map — "what's already true / where to look"), then only the code
  the feature migrates, scoped and by hand. It is not `explore-code` (recon of an *unfamiliar*
  repo) — a small spec'd project you just built is not unfamiliar, so no "map the architecture"
  fan-out on it. And delegating explore-agents *and* hand-reading the same files pays twice (the
  subagent exists to keep that reading out of your context — do one or the other, not both).
  (Live: a first-from-scratch Lane C task on a tiny fresh SPA burned ~50k tokens — two broad
  background explore-agents + ~10 hand-reads of the same surface — for one needed fact: the
  current serialized piece shape, ~2 files.)

- **v3.15: Worktrees are reaped, not just carved.** v3.9 said create one per problem and
  never said destroy it — so they accumulate silently (a session is blind to siblings'
  leftovers too). Reaping belongs to *task completion* (alongside
  `finishing-a-development-branch`): `git worktree remove` + `git branch -d` (which refuses
  if unmerged — that's the safety) + `prune`; scratchpad/detached trees die with their
  session. Audit by classifying, not counting (`merge-base --is-ancestor … origin/main`).
  (Live: gmb_v2 at 24 trees — 11 of 18 branch-worktrees fully merged = dead, plus 3 stale
  `/tmp` scratchpad trees; the handoff doc for the next session was invisible among them.)

- **v3.16: A sibling's code in your worktree is integration, not contamination.** Pulling
  others' commits is a shared trunk working as designed and can't be isolated away. Safety
  comes from an **invariant — `main` always green and deployable** — not from more
  isolation. Two corollaries agents get wrong: a deploy releases *the trunk*, not your
  feature ("my fix shipped by someone else's deploy" = correct); and a red `main` is not
  your problem alone — it hands risk to every session that deploys next. What *is*
  avoidable is interleaving inside the merge/deploy window → serialization (`flock`, one
  promoter), the Ops lane's job.

- **v3.17: Expedited ≠ untested — name the path, cut the right corner.** "Срочно, блокер у
  клиента" is legitimate and needs a declared lane, not a silent bypass. It skips the
  **soak** (demo dwell, review layer, openspec cycle → retro-spec after) and never the
  **suite + prod-smoke** — because under parallel sessions the suite is everyone else's net
  (v3.16). Mark it (`prod-<ts>-hotfix`) and land the commit on `develop` too so the skip is
  visible and `develop` doesn't fall behind. And if every release takes the fast path,
  that's an unnamed default, not an emergency — Lane D process consult.

- **v3.18: Заявка жнётся, а не бросается — и накопленный долг разгребается, а не сливается.**
  *Профилактика:* Lane C кончается на `openspec archive`, а не на «задачи отмечены» —
  archive это шаг, где дельта вливается в `openspec/specs/` и знание становится живой
  правдой. Жатва принадлежит завершению задачи (тот же момент, что снос ворктри в v3.15),
  не периодической уборке. Это v3.15 в другой обёртке: карвим и не жнём — только ворктри
  стоили читаемости `git worktree list`, а заявки стоят всей документации проекта.
  *Лечение (Lane C-drain):* archive — это **утверждение, что дельта истинна в коде**,
  значит вливать можно только после sweep'а. Ложная живая спека хуже тонкой: отсутствие
  документации честно, врущая — нет (и её читает Lane C explore, и её энфорсит
  `_conformance-sweep`). Триаж до фанаута (v3.10), группировка по capability — накопленный
  долг перекрывает сам себя, поздняя заявка молча вытесняет раннюю.
  (Боевое: gmb_v2 — 59 активных заявок против 11 заархивированных, 18 868 строк знания
  мимо 1 733 в живой спеке; 4 заявки с отметкой 100% задач так и не доехали; 7 заявок
  вокруг POS перекрывают друг друга.)

- **v3.19: Спека читается первой, иначе умирает правдивой.** v3.18 делает живую спеку
  правдивой — и этого мало: спека, которую никто не открывает, умирает второй раз, просто
  медленнее. Полоса Dx шла сразу в `systematic-debugging` (воспроизведи → корневая причина)
  и ни разу не говорила «сначала открой `openspec/specs/<capability>`» — агент грепал код
  по проекту, где спека уже отвечала на вопрос. Теперь: спека → `changes/` (кроме
  `archive/`) → и только потом код; **код против спеки = права спека**, пока не решено
  обратное сознательно (тогда это Lane C с обновлением SHALL, а не тихая правка).
  И ленивая ретро-адаптация: нет спеки → почини, затем `descriptive` SHALL на затронутую
  capability (не на весь seam — это `adopt-code`, он дороже). Спека растёт по трафику:
  документируется то, что реально ломается.
  (Источник: собственное правило из `~/.opencode/templates/PROJECT_AGENTS.md`, написанное
  для другого харнесса и не занесённое обратно в derflow.)

- **v3.20: openspec-скиллы подняты в глобальные — derflow ссылался на отсутствующее.**
  Движок Lane C называл `openspec-explore/propose/apply-change/archive-change/sync-specs`,
  а глобально их не было: жили только в `rk_bot/.claude/skills` и `mprz.ru/.github/skills`
  (побайтово одинаковые копии). В gmb_v2 — самом большом проекте — derflow указывал на
  несуществующие скиллы. CLI (`openspec` 1.5.0) стоял глобально, отсутствовала обучающая
  обвязка. Подняты пять скиллов + перенесён формат-референс `openspec-rules` (318 строк:
  шаблоны артефактов, жизненный цикл, «apply это не archive», восстановление после
  неверного архива, таблица «открывать change или править напрямую»).
  **И правка v3.19:** конвенция ID была записана неверно — `<YYYY-MM-DD>-<kebab>` взята
  из `PROJECT_AGENTS.md` без сверки с поведением инструмента. Правда обратная: активные ID
  **бездатные**, дату к архивной директории добавляет сам `openspec archive`, а CLI
  отвергает имена с цифры в начале. Следствие для C-drain усиливается: порядок заявок
  внутри capability берётся из git не «когда даты нет», а **всегда**.
  (Промах ровно того класса, против которого стоит внешне-контрактный гейт: утверждение
  о поведении инструмента взято из документа, а не из его собственной спецификации.)

- **v3.21: Ростер расширен — и маршрут не должен обещать больше, чем агент умеет.**
  Из `~/.config/opencode` перенесены шесть агентов той же коллекции `wshobson/agents`:
  `sql-pro` (ad-hoc SQL и медленные запросы — дыра, аналога не было), `docs-architect`
  (длинные архитектурные руководства по существующей системе), `reference-builder`
  (исчерпывающие справочники), `tutorial-engineer` (онбординг-гайды), `legal-advisor`,
  `payment-integration`. Один `documentation-expert` больше не отвечает за четыре разные
  роли. Проверяемая таблица «открывать change или править напрямую» приехала не сюда, а
  в `openspec-rules` (v3.20) — там ей место, а Lane C на неё ссылается.
  **Главный урок не в агентах, а в том, как их выбирали.** Два из шести обещают домен,
  которого не знают: `legal-advisor` написан под GDPR/EU (152-ФЗ, ОФД/ФНС — вне его),
  `payment-integration` под Stripe/PCI (СБП, ККТ, KkmServer — вне). Обнаружилось это
  только при чтении самих файлов: маршрутная таблица `workflow-cadence` в opencode
  объявляет `legal-advisor` агентом «узкого РФ-комплаенса», и я повторил это обещание,
  не открыв агента. **Описание агента в чужой маршрутной таблице — не контракт агента;
  контракт — его собственный промпт.** Ограничения продублированы в таблице
  дисамбигуации, чтобы derflow не отправлял к специалисту, которого нет.
  (Третий за сессию промах одного класса — вместе с конвенцией ID (v3.20) и вердиктом
  об SBP (v3.4): утверждение берётся из документа, описывающего вещь, вместо самой вещи.)
